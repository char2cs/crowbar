# Go
api/bin/
api/coverage/
api/logs/

# React / Node
node_modules/
web/node_modules/
web/dist/
web/.vite/

# Tauri / Rust
desktop/src-tauri/target/
desktop/src-tauri/binaries/

# Dev-instance Crowbar home (dev mode roots all state in the workspace, not ~/.crowbar)
/.crowbar/

# General
.DS_Store
*.env
*.env.local
*-trace.json

# go:embed staging copy of web/dist (generated by `make build`; not committed)
api/cmd/crowbar/web/
.superpowers/

# backend superpowers working artifacts (root docs/superpowers is intentionally tracked)
api/docs/superpowers/

# stray local build artifact (noEmbed daemon binary)
/api/crowbar
/api/crowbar-seed-chat

# hardening loop scratch (not committed)
/.hardening/

# icon build artifacts (generated by icons-compiler/compile-icon.sh from crowbar.icon source)
desktop/src-tauri/icons/Assets.car
desktop/src-tauri/icons/crowbar.icns
desktop/icons-compiler/.build/
