#![forbid(unsafe_code)]

//! CLI entry point for the parity differ. Invoked as
//! `cargo run -p oracle -- --report` (§12).

fn main() {
    println!("oracle: scaffold, no differ yet (native/QUEUE.md item 0.1)");
}
