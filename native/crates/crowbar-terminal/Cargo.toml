[package]
name = "crowbar-terminal"
description = "GPU text-grid renderer over the daemon's VT screen model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
crowbar-ui.workspace = true
crowbar-state.workspace = true
# §4.2 allows `ui` and `state` only. gpui is reached as `crowbar_ui::gpui` —
# crowbar-ui re-exports it, so this crate needs no manifest entry of its own.

[lints]
workspace = true
