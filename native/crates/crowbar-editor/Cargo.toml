[package]
name = "crowbar-editor"
description = "editor integration, buffers and retained editors"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
crowbar-ui.workspace = true
crowbar-state.workspace = true
crowbar-core.workspace = true
# gpui-component's `input` module (§10.1) is wrapped by crowbar-ui, which
# re-exports both `gpui` and `gpui_component`; reach them as
# `crowbar_ui::gpui` / `crowbar_ui::gpui_component`.

[lints]
workspace = true
