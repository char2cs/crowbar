[package]
name = "crowbar-state"
description = "Entity<T> stores, the event graph and subscription wiring"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
crowbar-core.workspace = true
crowbar-client.workspace = true
# §4.2: `core`, `client`, `gpui`. The §12 gate for this crate is `#[gpui::test]`
# on a seeded TestDispatcher; the `test-support` dev-dependency lands with the
# first such test, not before — it drags in a rev-pinned `proptest` git fork
# (vendor/PINNED.md) that nothing here needs yet.
gpui.workspace = true

[lints]
workspace = true
