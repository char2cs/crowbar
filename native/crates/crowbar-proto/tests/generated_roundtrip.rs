// @generated by native/tools/protogen — DO NOT EDIT
//
// Round-trip tests for the generated wire DTOs. Generated from the same IR as
// the DTOs themselves, so a Go handler cannot grow, lose or rename a field
// without this file changing in the same commit.
//
// 133 of 134 generated declarations are covered here.
//
// NOT covered, because no value of the type can be written without a
// dependency this crate does not have (spec §4.2 allows serde and nothing
// else). These declarations are not emitted into the crate either:
//   * github.com/gin-gonic/gin.H — lowers to serde_json::Value, and §4.2 gives this crate serde and nothing else
//
// The blanket allow is deliberate and is not hiding anything: this is
// machine-written test code with a machine's taste in naming and literals,
// and `clippy::pedantic` is denied workspace-wide (§4.3 rule 4). Style lints
// here would only ever be answered by editing the generator's whitespace.
#![allow(clippy::all, clippy::pedantic, non_snake_case)]

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/libs.Envelope`, fully populated.
#[test]
fn api_libs_envelope_wire_shape() {
    let value: crowbar_proto::api_libs::Envelope<String> = crowbar_proto::api_libs::Envelope {
        success: true,
        error: Some(String::from("s2")),
        data: Some(String::from("t3")),
    };
    let wire = r#"{"success": true, "error": "s2", "data": "t3"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_libs::Envelope<String> =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_libs::Envelope<String> =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/libs.Envelope` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_libs_envelope_zero_values() {
    let value: crowbar_proto::api_libs::Envelope<String> = crowbar_proto::api_libs::Envelope {
        success: false,
        error: None,
        data: None,
    };
    let wire = r#"{"success": false}"#;

    let parsed: crowbar_proto::api_libs::Envelope<String> =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_libs::Envelope<String> =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/libs.mutationData`, fully populated.
#[test]
fn api_libs_mutation_id_wire_shape() {
    let value: crowbar_proto::api_libs::MutationID = crowbar_proto::api_libs::MutationID {
        id: String::from("s1"),
    };
    let wire = r#"{"id": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_libs::MutationID =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_libs::MutationID =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/libs.mutationData` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_libs_mutation_id_zero_values() {
    let value: crowbar_proto::api_libs::MutationID =
        crowbar_proto::api_libs::MutationID { id: String::new() };
    let wire = r#"{"id": ""}"#;

    let parsed: crowbar_proto::api_libs::MutationID =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_libs::MutationID =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.AgentChatDTO`, fully populated.
#[test]
fn api_v0_dto_agent_chat_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::AgentChatDTO = crowbar_proto::api_v0_dto::AgentChatDTO {
        id: String::from("s1"),
        workspace_id: String::from("s2"),
        title: String::from("s3"),
        live_runner_id: String::from("s4"),
        terminal_session_id: String::from("s5"),
        active_provider_id: String::from("s6"),
        created_at: String::from("2026-07-31T05:00:00.000000007Z"),
    };
    let wire = r#"{"id": "s1", "workspaceId": "s2", "title": "s3", "liveRunnerId": "s4", "terminalSessionId": "s5", "activeProviderId": "s6", "createdAt": "2026-07-31T05:00:00.000000007Z"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::AgentChatDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::AgentChatDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.AgentChatDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_agent_chat_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::AgentChatDTO = crowbar_proto::api_v0_dto::AgentChatDTO {
        id: String::new(),
        workspace_id: String::new(),
        title: String::new(),
        live_runner_id: String::new(),
        terminal_session_id: String::new(),
        active_provider_id: String::new(),
        created_at: String::new(),
    };
    let wire = r#"{"id": "", "workspaceId": "", "title": "", "liveRunnerId": "", "terminalSessionId": "", "activeProviderId": "", "createdAt": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::AgentChatDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::AgentChatDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.AgentChatDetailDTO`, fully populated.
#[test]
fn api_v0_dto_agent_chat_detail_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::AgentChatDetailDTO =
        crowbar_proto::api_v0_dto::AgentChatDetailDTO {
            id: String::from("s1"),
            workspace_id: String::from("s2"),
            title: String::from("s3"),
            live_runner_id: String::from("s4"),
            terminal_session_id: String::from("s5"),
            active_provider_id: String::from("s6"),
            created_at: String::from("2026-07-31T05:00:00.000000007Z"),
            conversations: vec![crowbar_proto::domain::ChatConversation {
                chat_id: String::from("s8"),
                provider_id: String::from("s9"),
                session_id: String::from("s10"),
                first_seen_at: String::from("2026-07-31T05:00:00.000000011Z"),
            }],
        };
    let wire = r#"{"id": "s1", "workspaceId": "s2", "title": "s3", "liveRunnerId": "s4", "terminalSessionId": "s5", "activeProviderId": "s6", "createdAt": "2026-07-31T05:00:00.000000007Z", "conversations": [{"chatId": "s8", "providerId": "s9", "sessionId": "s10", "firstSeenAt": "2026-07-31T05:00:00.000000011Z"}]}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::AgentChatDetailDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::AgentChatDetailDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.AgentChatDetailDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_agent_chat_detail_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::AgentChatDetailDTO =
        crowbar_proto::api_v0_dto::AgentChatDetailDTO {
            id: String::new(),
            workspace_id: String::new(),
            title: String::new(),
            live_runner_id: String::new(),
            terminal_session_id: String::new(),
            active_provider_id: String::new(),
            created_at: String::new(),
            conversations: Vec::new(),
        };
    let wire = r#"{"id": "", "workspaceId": "", "title": "", "liveRunnerId": "", "terminalSessionId": "", "activeProviderId": "", "createdAt": "", "conversations": null}"#;

    let parsed: crowbar_proto::api_v0_dto::AgentChatDetailDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"id": "", "workspaceId": "", "title": "", "liveRunnerId": "", "terminalSessionId": "", "activeProviderId": "", "createdAt": "", "conversations": []}"#)
            .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::AgentChatDetailDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.AgentProviderDTO`, fully populated.
#[test]
fn api_v0_dto_agent_provider_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::AgentProviderDTO =
        crowbar_proto::api_v0_dto::AgentProviderDTO {
            id: String::from("s1"),
            display_name: String::from("s2"),
            icon: String::from("s3"),
            connected: false,
            enabled: true,
        };
    let wire =
        r#"{"id": "s1", "displayName": "s2", "icon": "s3", "connected": false, "enabled": true}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::AgentProviderDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::AgentProviderDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.AgentProviderDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_agent_provider_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::AgentProviderDTO =
        crowbar_proto::api_v0_dto::AgentProviderDTO {
            id: String::new(),
            display_name: String::new(),
            icon: String::new(),
            connected: false,
            enabled: false,
        };
    let wire = r#"{"id": "", "displayName": "", "icon": "", "connected": false, "enabled": false}"#;

    let parsed: crowbar_proto::api_v0_dto::AgentProviderDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::AgentProviderDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.BlameEntryDTO`, fully populated.
#[test]
fn api_v0_dto_blame_entry_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::BlameEntryDTO =
        crowbar_proto::api_v0_dto::BlameEntryDTO {
            line_number: 1,
            commit_hash: String::from("s2"),
            author: String::from("s3"),
            email: String::from("s4"),
            date: String::from("s5"),
            commit_message: String::from("s6"),
        };
    let wire = r#"{"lineNumber": 1, "commitHash": "s2", "author": "s3", "email": "s4", "date": "s5", "commitMessage": "s6"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::BlameEntryDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::BlameEntryDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.BlameEntryDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_blame_entry_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::BlameEntryDTO =
        crowbar_proto::api_v0_dto::BlameEntryDTO {
            line_number: 0,
            commit_hash: String::new(),
            author: String::new(),
            email: String::new(),
            date: String::new(),
            commit_message: String::new(),
        };
    let wire = r#"{"lineNumber": 0, "commitHash": "", "author": "", "email": "", "date": "", "commitMessage": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::BlameEntryDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::BlameEntryDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.BranchReviewDTO`, fully populated.
#[test]
fn api_v0_dto_branch_review_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::BranchReviewDTO =
        crowbar_proto::api_v0_dto::BranchReviewDTO {
            description: String::from("s1"),
            merge_strategy: String::from("s2"),
            threads: vec![crowbar_proto::api_v0_dto::ReviewThreadDTO {
                id: String::from("s3"),
                ws_id: String::from("s4"),
                file_path: String::from("s5"),
                line_number: 6,
                side: crowbar_proto::domain::ReviewSide::Left,
                status: crowbar_proto::domain::ReviewThreadStatus::Open,
                is_resolved: true,
                messages: vec![crowbar_proto::api_v0_dto::ReviewMessageDTO {
                    id: String::from("s8"),
                    author: Some(String::from("s9")),
                    is_agent: false,
                    body: String::from("s11"),
                    created_at: String::from("s12"),
                }],
                created_at: String::from("s13"),
            }],
        };
    let wire = r#"{"description": "s1", "mergeStrategy": "s2", "threads": [{"id": "s3", "wsId": "s4", "filePath": "s5", "lineNumber": 6, "side": "left", "status": "open", "isResolved": true, "messages": [{"id": "s8", "author": "s9", "isAgent": false, "body": "s11", "createdAt": "s12"}], "createdAt": "s13"}]}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::BranchReviewDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::BranchReviewDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.BranchReviewDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_branch_review_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::BranchReviewDTO =
        crowbar_proto::api_v0_dto::BranchReviewDTO {
            description: String::new(),
            merge_strategy: String::new(),
            threads: Vec::new(),
        };
    let wire = r#"{"description": "", "mergeStrategy": "", "threads": null}"#;

    let parsed: crowbar_proto::api_v0_dto::BranchReviewDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"description": "", "mergeStrategy": "", "threads": []}"#)
            .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::BranchReviewDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.GitFileDTO`, fully populated.
#[test]
fn api_v0_dto_git_file_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::GitFileDTO = crowbar_proto::api_v0_dto::GitFileDTO {
        path: String::from("s1"),
        status: String::from("s2"),
        staged: true,
    };
    let wire = r#"{"path": "s1", "status": "s2", "staged": true}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::GitFileDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::GitFileDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.GitFileDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_git_file_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::GitFileDTO = crowbar_proto::api_v0_dto::GitFileDTO {
        path: String::new(),
        status: String::new(),
        staged: false,
    };
    let wire = r#"{"path": "", "status": "", "staged": false}"#;

    let parsed: crowbar_proto::api_v0_dto::GitFileDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::GitFileDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.GitStatusDTO`, fully populated.
#[test]
fn api_v0_dto_git_status_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::GitStatusDTO = crowbar_proto::api_v0_dto::GitStatusDTO {
        branch: String::from("s1"),
        ahead: 2,
        behind: 3,
        files: vec![crowbar_proto::api_v0_dto::GitFileDTO {
            path: String::from("s4"),
            status: String::from("s5"),
            staged: false,
        }],
    };
    let wire = r#"{"branch": "s1", "ahead": 2, "behind": 3, "files": [{"path": "s4", "status": "s5", "staged": false}]}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::GitStatusDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::GitStatusDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.GitStatusDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_git_status_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::GitStatusDTO = crowbar_proto::api_v0_dto::GitStatusDTO {
        branch: String::new(),
        ahead: 0,
        behind: 0,
        files: Vec::new(),
    };
    let wire = r#"{"branch": "", "ahead": 0, "behind": 0, "files": null}"#;

    let parsed: crowbar_proto::api_v0_dto::GitStatusDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"branch": "", "ahead": 0, "behind": 0, "files": []}"#)
            .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::GitStatusDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.HandoffDTO`, fully populated.
#[test]
fn api_v0_dto_handoff_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::HandoffDTO = crowbar_proto::api_v0_dto::HandoffDTO {
        handoff: String::from("s1"),
    };
    let wire = r#"{"handoff": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::HandoffDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::HandoffDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.HandoffDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_handoff_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::HandoffDTO = crowbar_proto::api_v0_dto::HandoffDTO {
        handoff: String::new(),
    };
    let wire = r#"{"handoff": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::HandoffDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::HandoffDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPDidChangeRequest`, fully populated.
#[test]
fn api_v0_dto_lsp_did_change_request_wire_shape() {
    let value: crowbar_proto::api_v0_dto::LSPDidChangeRequest =
        crowbar_proto::api_v0_dto::LSPDidChangeRequest {
            path: String::from("s1"),
            text: String::from("s2"),
        };
    let wire = r#"{"path": "s1", "text": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::LSPDidChangeRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPDidChangeRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPDidChangeRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_lsp_did_change_request_zero_values() {
    let value: crowbar_proto::api_v0_dto::LSPDidChangeRequest =
        crowbar_proto::api_v0_dto::LSPDidChangeRequest {
            path: String::new(),
            text: String::new(),
        };
    let wire = r#"{"path": "", "text": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::LSPDidChangeRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPDidChangeRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPDidOpenRequest`, fully populated.
#[test]
fn api_v0_dto_lsp_did_open_request_wire_shape() {
    let value: crowbar_proto::api_v0_dto::LSPDidOpenRequest =
        crowbar_proto::api_v0_dto::LSPDidOpenRequest {
            path: String::from("s1"),
            language_id: String::from("s2"),
            text: String::from("s3"),
        };
    let wire = r#"{"path": "s1", "languageId": "s2", "text": "s3"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::LSPDidOpenRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPDidOpenRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPDidOpenRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_lsp_did_open_request_zero_values() {
    let value: crowbar_proto::api_v0_dto::LSPDidOpenRequest =
        crowbar_proto::api_v0_dto::LSPDidOpenRequest {
            path: String::new(),
            language_id: String::new(),
            text: String::new(),
        };
    let wire = r#"{"path": "", "languageId": "", "text": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::LSPDidOpenRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPDidOpenRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPPathRequest`, fully populated.
#[test]
fn api_v0_dto_lsp_path_request_wire_shape() {
    let value: crowbar_proto::api_v0_dto::LSPPathRequest =
        crowbar_proto::api_v0_dto::LSPPathRequest {
            path: String::from("s1"),
        };
    let wire = r#"{"path": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::LSPPathRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPPathRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPPathRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_lsp_path_request_zero_values() {
    let value: crowbar_proto::api_v0_dto::LSPPathRequest =
        crowbar_proto::api_v0_dto::LSPPathRequest {
            path: String::new(),
        };
    let wire = r#"{"path": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::LSPPathRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPPathRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPPositionRequest`, fully populated.
#[test]
fn api_v0_dto_lsp_position_request_wire_shape() {
    let value: crowbar_proto::api_v0_dto::LSPPositionRequest =
        crowbar_proto::api_v0_dto::LSPPositionRequest {
            path: String::from("s1"),
            position: crowbar_proto::domain_lsp::Position {
                line: 2,
                character: 3,
            },
        };
    let wire = r#"{"path": "s1", "position": {"line": 2, "character": 3}}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::LSPPositionRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPPositionRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPPositionRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_lsp_position_request_zero_values() {
    let value: crowbar_proto::api_v0_dto::LSPPositionRequest =
        crowbar_proto::api_v0_dto::LSPPositionRequest {
            path: String::new(),
            position: crowbar_proto::domain_lsp::Position {
                line: 0,
                character: 0,
            },
        };
    let wire = r#"{"path": "", "position": {"line": 0, "character": 0}}"#;

    let parsed: crowbar_proto::api_v0_dto::LSPPositionRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPPositionRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPRangeRequest`, fully populated.
#[test]
fn api_v0_dto_lsp_range_request_wire_shape() {
    let value: crowbar_proto::api_v0_dto::LSPRangeRequest =
        crowbar_proto::api_v0_dto::LSPRangeRequest {
            path: String::from("s1"),
            range: crowbar_proto::domain_lsp::Range {
                start: crowbar_proto::domain_lsp::Position {
                    line: 2,
                    character: 3,
                },
                end: crowbar_proto::domain_lsp::Position {
                    line: 4,
                    character: 5,
                },
            },
        };
    let wire = r#"{"path": "s1", "range": {"start": {"line": 2, "character": 3}, "end": {"line": 4, "character": 5}}}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::LSPRangeRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPRangeRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPRangeRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_lsp_range_request_zero_values() {
    let value: crowbar_proto::api_v0_dto::LSPRangeRequest =
        crowbar_proto::api_v0_dto::LSPRangeRequest {
            path: String::new(),
            range: crowbar_proto::domain_lsp::Range {
                start: crowbar_proto::domain_lsp::Position {
                    line: 0,
                    character: 0,
                },
                end: crowbar_proto::domain_lsp::Position {
                    line: 0,
                    character: 0,
                },
            },
        };
    let wire = r#"{"path": "", "range": {"start": {"line": 0, "character": 0}, "end": {"line": 0, "character": 0}}}"#;

    let parsed: crowbar_proto::api_v0_dto::LSPRangeRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPRangeRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPRenameRequest`, fully populated.
#[test]
fn api_v0_dto_lsp_rename_request_wire_shape() {
    let value: crowbar_proto::api_v0_dto::LSPRenameRequest =
        crowbar_proto::api_v0_dto::LSPRenameRequest {
            path: String::from("s1"),
            position: crowbar_proto::domain_lsp::Position {
                line: 2,
                character: 3,
            },
            new_name: String::from("s4"),
        };
    let wire = r#"{"path": "s1", "position": {"line": 2, "character": 3}, "newName": "s4"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::LSPRenameRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPRenameRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.LSPRenameRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_lsp_rename_request_zero_values() {
    let value: crowbar_proto::api_v0_dto::LSPRenameRequest =
        crowbar_proto::api_v0_dto::LSPRenameRequest {
            path: String::new(),
            position: crowbar_proto::domain_lsp::Position {
                line: 0,
                character: 0,
            },
            new_name: String::new(),
        };
    let wire = r#"{"path": "", "position": {"line": 0, "character": 0}, "newName": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::LSPRenameRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::LSPRenameRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.ProjectDTO`, fully populated.
#[test]
fn api_v0_dto_project_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::ProjectDTO = crowbar_proto::api_v0_dto::ProjectDTO {
        id: String::from("s1"),
        name: String::from("s2"),
        path: String::from("s3"),
        status: Some(String::from("s4")),
        last_activity: String::from("2026-07-31T05:00:00.000000005Z"),
    };
    let wire = r#"{"id": "s1", "name": "s2", "path": "s3", "status": "s4", "lastActivity": "2026-07-31T05:00:00.000000005Z"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::ProjectDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::ProjectDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.ProjectDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_project_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::ProjectDTO = crowbar_proto::api_v0_dto::ProjectDTO {
        id: String::new(),
        name: String::new(),
        path: String::new(),
        status: None,
        last_activity: String::new(),
    };
    let wire = r#"{"id": "", "name": "", "path": "", "lastActivity": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::ProjectDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::ProjectDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.RepoDTO`, fully populated.
#[test]
fn api_v0_dto_repo_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::RepoDTO = crowbar_proto::api_v0_dto::RepoDTO {
        id: String::from("s1"),
        project_id: String::from("s2"),
        name: String::from("s3"),
        path: String::from("s4"),
        default_branch: String::from("s5"),
        avatar_label: String::from("s6"),
        avatar_color: String::from("s7"),
        avatar_url: Some(String::from("s8")),
        avatar_emoji: Some(String::from("s9")),
        status: Some(String::from("s10")),
    };
    let wire = r#"{"id": "s1", "projectId": "s2", "name": "s3", "path": "s4", "defaultBranch": "s5", "avatarLabel": "s6", "avatarColor": "s7", "avatarUrl": "s8", "avatarEmoji": "s9", "status": "s10"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::RepoDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::RepoDTO = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.RepoDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_repo_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::RepoDTO = crowbar_proto::api_v0_dto::RepoDTO {
        id: String::new(),
        project_id: String::new(),
        name: String::new(),
        path: String::new(),
        default_branch: String::new(),
        avatar_label: String::new(),
        avatar_color: String::new(),
        avatar_url: None,
        avatar_emoji: None,
        status: None,
    };
    let wire = r#"{"id": "", "projectId": "", "name": "", "path": "", "defaultBranch": "", "avatarLabel": "", "avatarColor": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::RepoDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::RepoDTO = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.ReviewMessageDTO`, fully populated.
#[test]
fn api_v0_dto_review_message_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::ReviewMessageDTO =
        crowbar_proto::api_v0_dto::ReviewMessageDTO {
            id: String::from("s1"),
            author: Some(String::from("s2")),
            is_agent: true,
            body: String::from("s4"),
            created_at: String::from("s5"),
        };
    let wire = r#"{"id": "s1", "author": "s2", "isAgent": true, "body": "s4", "createdAt": "s5"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::ReviewMessageDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::ReviewMessageDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.ReviewMessageDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_review_message_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::ReviewMessageDTO =
        crowbar_proto::api_v0_dto::ReviewMessageDTO {
            id: String::new(),
            author: None,
            is_agent: false,
            body: String::new(),
            created_at: String::new(),
        };
    let wire = r#"{"id": "", "isAgent": false, "body": "", "createdAt": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::ReviewMessageDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::ReviewMessageDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.ReviewThreadDTO`, fully populated.
#[test]
fn api_v0_dto_review_thread_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::ReviewThreadDTO =
        crowbar_proto::api_v0_dto::ReviewThreadDTO {
            id: String::from("s1"),
            ws_id: String::from("s2"),
            file_path: String::from("s3"),
            line_number: 4,
            side: crowbar_proto::domain::ReviewSide::Left,
            status: crowbar_proto::domain::ReviewThreadStatus::Open,
            is_resolved: true,
            messages: vec![crowbar_proto::api_v0_dto::ReviewMessageDTO {
                id: String::from("s6"),
                author: Some(String::from("s7")),
                is_agent: false,
                body: String::from("s9"),
                created_at: String::from("s10"),
            }],
            created_at: String::from("s11"),
        };
    let wire = r#"{"id": "s1", "wsId": "s2", "filePath": "s3", "lineNumber": 4, "side": "left", "status": "open", "isResolved": true, "messages": [{"id": "s6", "author": "s7", "isAgent": false, "body": "s9", "createdAt": "s10"}], "createdAt": "s11"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::ReviewThreadDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::ReviewThreadDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.ReviewThreadDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_review_thread_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::ReviewThreadDTO =
        crowbar_proto::api_v0_dto::ReviewThreadDTO {
            id: String::new(),
            ws_id: String::new(),
            file_path: String::new(),
            line_number: 0,
            side: crowbar_proto::domain::ReviewSide::Other(String::new()),
            status: crowbar_proto::domain::ReviewThreadStatus::Other(String::new()),
            is_resolved: false,
            messages: Vec::new(),
            created_at: String::new(),
        };
    let wire = r#"{"id": "", "wsId": "", "filePath": "", "lineNumber": 0, "side": "", "status": "", "isResolved": false, "messages": null, "createdAt": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::ReviewThreadDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"id": "", "wsId": "", "filePath": "", "lineNumber": 0, "side": "", "status": "", "isResolved": false, "messages": [], "createdAt": ""}"#)
            .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::ReviewThreadDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.TerminalSessionDTO`, fully populated.
#[test]
fn api_v0_dto_terminal_session_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::TerminalSessionDTO =
        crowbar_proto::api_v0_dto::TerminalSessionDTO {
            id: String::from("s1"),
            project_id: String::from("s2"),
            repo_id: String::from("s3"),
            workspace_id: String::from("s4"),
            profile_id: Some(String::from("s5")),
            status: String::from("s6"),
            exit_code: Some(7),
            created_at: String::from("2026-07-31T05:00:00.000000008Z"),
            ended_at: Some(String::from("2026-07-31T05:00:00.000000009Z")),
        };
    let wire = r#"{"id": "s1", "projectId": "s2", "repoId": "s3", "workspaceId": "s4", "profileId": "s5", "status": "s6", "exitCode": 7, "createdAt": "2026-07-31T05:00:00.000000008Z", "endedAt": "2026-07-31T05:00:00.000000009Z"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::TerminalSessionDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::TerminalSessionDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.TerminalSessionDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_terminal_session_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::TerminalSessionDTO =
        crowbar_proto::api_v0_dto::TerminalSessionDTO {
            id: String::new(),
            project_id: String::new(),
            repo_id: String::new(),
            workspace_id: String::new(),
            profile_id: None,
            status: String::new(),
            exit_code: None,
            created_at: String::new(),
            ended_at: None,
        };
    let wire = r#"{"id": "", "projectId": "", "repoId": "", "workspaceId": "", "status": "", "createdAt": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::TerminalSessionDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::TerminalSessionDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.ThreadDTO`, fully populated.
#[test]
fn api_v0_dto_thread_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::ThreadDTO = crowbar_proto::api_v0_dto::ThreadDTO {
        id: String::from("s1"),
        project_id: String::from("s2"),
        repo_id: String::from("s3"),
        workspace_id: String::from("s4"),
        file_path: String::from("s5"),
        line: 6,
        start_line: 7,
        end_line: 8,
        side: String::from("s9"),
        message_id: String::from("s10"),
        body: String::from("s11"),
        author: String::from("s12"),
        is_agent: true,
        resolved: false,
        created_at: String::from("2026-07-31T05:00:00.000000015Z"),
        replies: vec![crowbar_proto::api_v0_dto::ThreadReplyDTO {
            id: String::from("s16"),
            thread_id: String::from("s17"),
            body: String::from("s18"),
            author: String::from("s19"),
            is_agent: false,
            created_at: String::from("2026-07-31T05:00:00.000000021Z"),
        }],
        deleted: Some(false),
    };
    let wire = r#"{"id": "s1", "projectId": "s2", "repoId": "s3", "workspaceId": "s4", "filePath": "s5", "line": 6, "startLine": 7, "endLine": 8, "side": "s9", "messageId": "s10", "body": "s11", "author": "s12", "isAgent": true, "resolved": false, "createdAt": "2026-07-31T05:00:00.000000015Z", "replies": [{"id": "s16", "threadId": "s17", "body": "s18", "author": "s19", "isAgent": false, "createdAt": "2026-07-31T05:00:00.000000021Z"}], "deleted": false}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::ThreadDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::ThreadDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.ThreadDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_thread_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::ThreadDTO = crowbar_proto::api_v0_dto::ThreadDTO {
        id: String::new(),
        project_id: String::new(),
        repo_id: String::new(),
        workspace_id: String::new(),
        file_path: String::new(),
        line: 0,
        start_line: 0,
        end_line: 0,
        side: String::new(),
        message_id: String::new(),
        body: String::new(),
        author: String::new(),
        is_agent: false,
        resolved: false,
        created_at: String::new(),
        replies: Vec::new(),
        deleted: None,
    };
    let wire = r#"{"id": "", "projectId": "", "repoId": "", "workspaceId": "", "filePath": "", "line": 0, "startLine": 0, "endLine": 0, "side": "", "messageId": "", "body": "", "author": "", "isAgent": false, "resolved": false, "createdAt": "", "replies": null}"#;

    let parsed: crowbar_proto::api_v0_dto::ThreadDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"id": "", "projectId": "", "repoId": "", "workspaceId": "", "filePath": "", "line": 0, "startLine": 0, "endLine": 0, "side": "", "messageId": "", "body": "", "author": "", "isAgent": false, "resolved": false, "createdAt": "", "replies": []}"#)
            .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::ThreadDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.ThreadReplyDTO`, fully populated.
#[test]
fn api_v0_dto_thread_reply_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::ThreadReplyDTO =
        crowbar_proto::api_v0_dto::ThreadReplyDTO {
            id: String::from("s1"),
            thread_id: String::from("s2"),
            body: String::from("s3"),
            author: String::from("s4"),
            is_agent: true,
            created_at: String::from("2026-07-31T05:00:00.000000006Z"),
        };
    let wire = r#"{"id": "s1", "threadId": "s2", "body": "s3", "author": "s4", "isAgent": true, "createdAt": "2026-07-31T05:00:00.000000006Z"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::ThreadReplyDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::ThreadReplyDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.ThreadReplyDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_thread_reply_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::ThreadReplyDTO =
        crowbar_proto::api_v0_dto::ThreadReplyDTO {
            id: String::new(),
            thread_id: String::new(),
            body: String::new(),
            author: String::new(),
            is_agent: false,
            created_at: String::new(),
        };
    let wire = r#"{"id": "", "threadId": "", "body": "", "author": "", "isAgent": false, "createdAt": ""}"#;

    let parsed: crowbar_proto::api_v0_dto::ThreadReplyDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::ThreadReplyDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/dto.WorkspaceDTO`, fully populated.
#[test]
fn api_v0_dto_workspace_dto_wire_shape() {
    let value: crowbar_proto::api_v0_dto::WorkspaceDTO = crowbar_proto::api_v0_dto::WorkspaceDTO {
        id: String::from("s1"),
        repo_id: String::from("s2"),
        project_id: String::from("s3"),
        kind: Some(crowbar_proto::domain::WorkspaceKind::Git),
        branch: String::from("s4"),
        parent_id: Some(String::from("s5")),
        fork_point_sha: Some(String::from("s6")),
        status: Some(crowbar_proto::domain::WorkspaceStatus::Deleted),
        working: true,
        last_error: Some(String::from("s8")),
        is_default: Some(true),
        added: 10,
        deleted: 11,
        merge_strategy: crowbar_proto::domain_git::MergeStrategy::Merge,
        can_merge_locally: false,
        merge_conflicts: true,
        parent_branch: Some(String::from("s14")),
        pr_url: Some(String::from("s15")),
        pr_title: Some(String::from("s16")),
        pr_target_branch: Some(String::from("s17")),
        local_path: Some(String::from("s18")),
        held_by_path: Some(String::from("s19")),
    };
    let wire = r#"{"id": "s1", "repoId": "s2", "projectId": "s3", "kind": "git", "branch": "s4", "parentId": "s5", "forkPointSha": "s6", "status": "deleted", "working": true, "lastError": "s8", "isDefault": true, "added": 10, "deleted": 11, "mergeStrategy": "merge", "canMergeLocally": false, "mergeConflicts": true, "parentBranch": "s14", "prUrl": "s15", "prTitle": "s16", "prTargetBranch": "s17", "localPath": "s18", "heldByPath": "s19"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_dto::WorkspaceDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::WorkspaceDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/dto.WorkspaceDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_dto_workspace_dto_zero_values() {
    let value: crowbar_proto::api_v0_dto::WorkspaceDTO = crowbar_proto::api_v0_dto::WorkspaceDTO {
        id: String::new(),
        repo_id: String::new(),
        project_id: String::new(),
        kind: None,
        branch: String::new(),
        parent_id: None,
        fork_point_sha: None,
        status: None,
        working: false,
        last_error: None,
        is_default: None,
        added: 0,
        deleted: 0,
        merge_strategy: crowbar_proto::domain_git::MergeStrategy::Other(String::new()),
        can_merge_locally: false,
        merge_conflicts: false,
        parent_branch: None,
        pr_url: None,
        pr_title: None,
        pr_target_branch: None,
        local_path: None,
        held_by_path: None,
    };
    let wire = r#"{"id": "", "repoId": "", "projectId": "", "branch": "", "working": false, "added": 0, "deleted": 0, "mergeStrategy": "", "canMergeLocally": false, "mergeConflicts": false}"#;

    let parsed: crowbar_proto::api_v0_dto::WorkspaceDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_dto::WorkspaceDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Create request>`, fully populated.
#[test]
fn api_v0_endpoints_agent_handlers_create_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest {
            provider: String::from("s1"),
        };
    let wire = r#"{"provider": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Create request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_agent_handlers_create_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest {
            provider: String::new(),
        };
    let wire = r#"{"provider": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Create request>`, fully populated.
#[test]
fn api_v0_endpoints_agent_handlers_create_request2_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest2 =
        crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest2 {
            provider: String::from("s1"),
        };
    let wire = r#"{"provider": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest2 =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Create request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_agent_handlers_create_request2_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest2 =
        crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest2 {
            provider: String::new(),
        };
    let wire = r#"{"provider": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest2 =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::CreateRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Hooks request>`, fully populated.
#[test]
fn api_v0_endpoints_agent_handlers_hooks_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest {
            segment_id: String::from("s1"),
            provider: String::from("s2"),
            event: String::from("s3"),
            payload_raw: String::from("s4"),
        };
    let wire = r#"{"segment_id": "s1", "provider": "s2", "event": "s3", "payload_raw": "s4"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Hooks request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_agent_handlers_hooks_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest {
            segment_id: String::new(),
            provider: String::new(),
            event: String::new(),
            payload_raw: String::new(),
        };
    let wire = r#"{"segment_id": "", "provider": "", "event": "", "payload_raw": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Hooks request>`, fully populated.
#[test]
fn api_v0_endpoints_agent_handlers_hooks_request2_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest2 =
        crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest2 {
            segment_id: String::from("s1"),
            provider: String::from("s2"),
            event: String::from("s3"),
            payload_raw: String::from("s4"),
        };
    let wire = r#"{"segment_id": "s1", "provider": "s2", "event": "s3", "payload_raw": "s4"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest2 =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Hooks request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_agent_handlers_hooks_request2_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest2 =
        crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest2 {
            segment_id: String::new(),
            provider: String::new(),
            event: String::new(),
            payload_raw: String::new(),
        };
    let wire = r#"{"segment_id": "", "provider": "", "event": "", "payload_raw": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest2 =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::HooksRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous RenameByRunner request>`, fully populated.
#[test]
fn api_v0_endpoints_agent_handlers_rename_by_runner_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest {
            title: String::from("s1"),
        };
    let wire = r#"{"title": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous RenameByRunner request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_agent_handlers_rename_by_runner_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest {
            title: String::new(),
        };
    let wire = r#"{"title": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous RenameByRunner request>`, fully populated.
#[test]
fn api_v0_endpoints_agent_handlers_rename_by_runner_request2_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest2 =
        crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest2 {
            title: String::from("s1"),
        };
    let wire = r#"{"title": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest2 =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous RenameByRunner request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_agent_handlers_rename_by_runner_request2_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest2 =
        crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest2 {
            title: String::new(),
        };
    let wire = r#"{"title": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest2 =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::RenameByRunnerRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Rename request>`, fully populated.
#[test]
fn api_v0_endpoints_agent_handlers_rename_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest {
            title: String::from("s1"),
        };
    let wire = r#"{"title": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Rename request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_agent_handlers_rename_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest {
            title: String::new(),
        };
    let wire = r#"{"title": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Rename request>`, fully populated.
#[test]
fn api_v0_endpoints_agent_handlers_rename_request2_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest2 =
        crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest2 {
            title: String::from("s1"),
        };
    let wire = r#"{"title": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest2 =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Rename request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_agent_handlers_rename_request2_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest2 =
        crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest2 {
            title: String::new(),
        };
    let wire = r#"{"title": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest2 =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::RenameRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Switch request>`, fully populated.
#[test]
fn api_v0_endpoints_agent_handlers_switch_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest {
            provider: String::from("s1"),
        };
    let wire = r#"{"provider": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Switch request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_agent_handlers_switch_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest {
            provider: String::new(),
        };
    let wire = r#"{"provider": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Switch request>`, fully populated.
#[test]
fn api_v0_endpoints_agent_handlers_switch_request2_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest2 =
        crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest2 {
            provider: String::from("s1"),
        };
    let wire = r#"{"provider": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest2 =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous Switch request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_agent_handlers_switch_request2_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest2 =
        crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest2 {
            provider: String::new(),
        };
    let wire = r#"{"provider": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest2 =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::SwitchRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous UpdateProviderPreferences request>`, fully populated.
#[test]
fn api_v0_endpoints_agent_handlers_update_provider_preferences_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::UpdateProviderPreferencesRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::UpdateProviderPreferencesRequest {};
    let wire = r#"{}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::UpdateProviderPreferencesRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::UpdateProviderPreferencesRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/agent/handlers.<anonymous UpdateProviderPreferences request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_agent_handlers_update_provider_preferences_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_agent_handlers::UpdateProviderPreferencesRequest =
        crowbar_proto::api_v0_endpoints_agent_handlers::UpdateProviderPreferencesRequest {};
    let wire = r#"{}"#;

    let parsed: crowbar_proto::api_v0_endpoints_agent_handlers::UpdateProviderPreferencesRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_agent_handlers::UpdateProviderPreferencesRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/editor/handlers.syncOK`, fully populated.
#[test]
fn api_v0_endpoints_editor_handlers_sync_ok_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_editor_handlers::SyncOK =
        crowbar_proto::api_v0_endpoints_editor_handlers::SyncOK { ok: true };
    let wire = r#"{"ok": true}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_editor_handlers::SyncOK =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_editor_handlers::SyncOK =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/editor/handlers.syncOK` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_editor_handlers_sync_ok_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_editor_handlers::SyncOK =
        crowbar_proto::api_v0_endpoints_editor_handlers::SyncOK { ok: false };
    let wire = r#"{"ok": false}"#;

    let parsed: crowbar_proto::api_v0_endpoints_editor_handlers::SyncOK =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_editor_handlers::SyncOK =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/files/handlers.<anonymous Copy request>`, fully populated.
#[test]
fn api_v0_endpoints_files_handlers_copy_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_files_handlers::CopyRequest =
        crowbar_proto::api_v0_endpoints_files_handlers::CopyRequest {
            source_path: String::from("s1"),
            dest_path: String::from("s2"),
        };
    let wire = r#"{"sourcePath": "s1", "destPath": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_files_handlers::CopyRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_files_handlers::CopyRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/files/handlers.<anonymous Copy request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_files_handlers_copy_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_files_handlers::CopyRequest =
        crowbar_proto::api_v0_endpoints_files_handlers::CopyRequest {
            source_path: String::new(),
            dest_path: String::new(),
        };
    let wire = r#"{"sourcePath": "", "destPath": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_files_handlers::CopyRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_files_handlers::CopyRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/files/handlers.<anonymous Create request>`, fully populated.
#[test]
fn api_v0_endpoints_files_handlers_create_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_files_handlers::CreateRequest =
        crowbar_proto::api_v0_endpoints_files_handlers::CreateRequest {
            path: String::from("s1"),
            type_: String::from("s2"),
        };
    let wire = r#"{"path": "s1", "type": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_files_handlers::CreateRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_files_handlers::CreateRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/files/handlers.<anonymous Create request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_files_handlers_create_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_files_handlers::CreateRequest =
        crowbar_proto::api_v0_endpoints_files_handlers::CreateRequest {
            path: String::new(),
            type_: String::new(),
        };
    let wire = r#"{"path": "", "type": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_files_handlers::CreateRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_files_handlers::CreateRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/files/handlers.<anonymous Delete request>`, fully populated.
#[test]
fn api_v0_endpoints_files_handlers_delete_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_files_handlers::DeleteRequest =
        crowbar_proto::api_v0_endpoints_files_handlers::DeleteRequest {
            path: String::from("s1"),
        };
    let wire = r#"{"path": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_files_handlers::DeleteRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_files_handlers::DeleteRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/files/handlers.<anonymous Delete request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_files_handlers_delete_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_files_handlers::DeleteRequest =
        crowbar_proto::api_v0_endpoints_files_handlers::DeleteRequest {
            path: String::new(),
        };
    let wire = r#"{"path": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_files_handlers::DeleteRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_files_handlers::DeleteRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/files/handlers.<anonymous Rename request>`, fully populated.
#[test]
fn api_v0_endpoints_files_handlers_rename_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_files_handlers::RenameRequest =
        crowbar_proto::api_v0_endpoints_files_handlers::RenameRequest {
            path: String::from("s1"),
            new_path: String::from("s2"),
        };
    let wire = r#"{"path": "s1", "newPath": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_files_handlers::RenameRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_files_handlers::RenameRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/files/handlers.<anonymous Rename request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_files_handlers_rename_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_files_handlers::RenameRequest =
        crowbar_proto::api_v0_endpoints_files_handlers::RenameRequest {
            path: String::new(),
            new_path: String::new(),
        };
    let wire = r#"{"path": "", "newPath": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_files_handlers::RenameRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_files_handlers::RenameRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/files/handlers.<anonymous SaveContent request>`, fully populated.
#[test]
fn api_v0_endpoints_files_handlers_save_content_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_files_handlers::SaveContentRequest =
        crowbar_proto::api_v0_endpoints_files_handlers::SaveContentRequest {
            path: String::from("s1"),
            content: String::from("s2"),
            encoding: String::from("s3"),
        };
    let wire = r#"{"path": "s1", "content": "s2", "encoding": "s3"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_files_handlers::SaveContentRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_files_handlers::SaveContentRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/files/handlers.<anonymous SaveContent request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_files_handlers_save_content_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_files_handlers::SaveContentRequest =
        crowbar_proto::api_v0_endpoints_files_handlers::SaveContentRequest {
            path: String::new(),
            content: String::new(),
            encoding: String::new(),
        };
    let wire = r#"{"path": "", "content": "", "encoding": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_files_handlers::SaveContentRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_files_handlers::SaveContentRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous Commit request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_commit_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::CommitRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::CommitRequest {
            subject: String::from("s1"),
            body: String::from("s2"),
            author: String::from("s3"),
        };
    let wire = r#"{"subject": "s1", "body": "s2", "author": "s3"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::CommitRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::CommitRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous Commit request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_commit_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::CommitRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::CommitRequest {
            subject: String::new(),
            body: String::new(),
            author: String::new(),
        };
    let wire = r#"{"subject": "", "body": "", "author": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::CommitRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::CommitRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous CreateBranch request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_create_branch_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::CreateBranchRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::CreateBranchRequest {
            name: String::from("s1"),
        };
    let wire = r#"{"name": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::CreateBranchRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::CreateBranchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous CreateBranch request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_create_branch_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::CreateBranchRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::CreateBranchRequest {
            name: String::new(),
        };
    let wire = r#"{"name": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::CreateBranchRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::CreateBranchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous DeleteBranch request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_delete_branch_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::DeleteBranchRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::DeleteBranchRequest {
            name: String::from("s1"),
        };
    let wire = r#"{"name": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::DeleteBranchRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::DeleteBranchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous DeleteBranch request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_delete_branch_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::DeleteBranchRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::DeleteBranchRequest {
            name: String::new(),
        };
    let wire = r#"{"name": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::DeleteBranchRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::DeleteBranchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous Merge request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_merge_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::MergeRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::MergeRequest {
            branch: String::from("s1"),
        };
    let wire = r#"{"branch": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::MergeRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::MergeRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous Merge request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_merge_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::MergeRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::MergeRequest {
            branch: String::new(),
        };
    let wire = r#"{"branch": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::MergeRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::MergeRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.pathsBody`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_paths_body_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::PathsBody =
        crowbar_proto::api_v0_endpoints_git_handlers::PathsBody {
            paths: vec![String::from("s1")],
        };
    let wire = r#"{"paths": ["s1"]}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::PathsBody =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::PathsBody =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.pathsBody` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_paths_body_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::PathsBody =
        crowbar_proto::api_v0_endpoints_git_handlers::PathsBody { paths: Vec::new() };
    let wire = r#"{"paths": null}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::PathsBody =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"paths": []}"#).expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::PathsBody =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous Rebase request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_rebase_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::RebaseRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::RebaseRequest {
            branch: String::from("s1"),
        };
    let wire = r#"{"branch": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::RebaseRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::RebaseRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous Rebase request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_rebase_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::RebaseRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::RebaseRequest {
            branch: String::new(),
        };
    let wire = r#"{"branch": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::RebaseRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::RebaseRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous RenameBranch request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_rename_branch_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::RenameBranchRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::RenameBranchRequest {
            from: String::from("s1"),
            to: String::from("s2"),
        };
    let wire = r#"{"from": "s1", "to": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::RenameBranchRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::RenameBranchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous RenameBranch request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_rename_branch_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::RenameBranchRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::RenameBranchRequest {
            from: String::new(),
            to: String::new(),
        };
    let wire = r#"{"from": "", "to": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::RenameBranchRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::RenameBranchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous Reset request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_reset_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::ResetRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::ResetRequest {
            mode: String::from("s1"),
        };
    let wire = r#"{"mode": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::ResetRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::ResetRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous Reset request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_reset_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::ResetRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::ResetRequest {
            mode: String::new(),
        };
    let wire = r#"{"mode": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::ResetRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::ResetRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous ResolveHunk request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_resolve_hunk_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::ResolveHunkRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::ResolveHunkRequest {
            path: String::from("s1"),
            hunk_index: 2,
            choice: String::from("s3"),
            resolved_content: String::from("s4"),
        };
    let wire = r#"{"path": "s1", "hunkIndex": 2, "choice": "s3", "resolvedContent": "s4"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::ResolveHunkRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::ResolveHunkRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous ResolveHunk request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_resolve_hunk_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::ResolveHunkRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::ResolveHunkRequest {
            path: String::new(),
            hunk_index: 0,
            choice: String::new(),
            resolved_content: String::new(),
        };
    let wire = r#"{"path": "", "hunkIndex": 0, "choice": "", "resolvedContent": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::ResolveHunkRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::ResolveHunkRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous StageHunk request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_stage_hunk_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::StageHunkRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::StageHunkRequest {
            path: String::from("s1"),
            hunk_id: String::from("s2"),
        };
    let wire = r#"{"path": "s1", "hunkId": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::StageHunkRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::StageHunkRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous StageHunk request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_stage_hunk_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::StageHunkRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::StageHunkRequest {
            path: String::new(),
            hunk_id: String::new(),
        };
    let wire = r#"{"path": "", "hunkId": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::StageHunkRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::StageHunkRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous StashApply request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_stash_apply_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::StashApplyRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::StashApplyRequest { index: 1 };
    let wire = r#"{"index": 1}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::StashApplyRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::StashApplyRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous StashApply request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_stash_apply_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::StashApplyRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::StashApplyRequest { index: 0 };
    let wire = r#"{"index": 0}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::StashApplyRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::StashApplyRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous StashDrop request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_stash_drop_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::StashDropRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::StashDropRequest { index: Some(1) };
    let wire = r#"{"index": 1}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::StashDropRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::StashDropRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous StashDrop request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_stash_drop_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::StashDropRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::StashDropRequest { index: None };
    let wire = r#"{}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::StashDropRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::StashDropRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous StashPop request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_stash_pop_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::StashPopRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::StashPopRequest { index: 1 };
    let wire = r#"{"index": 1}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::StashPopRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::StashPopRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous StashPop request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_stash_pop_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::StashPopRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::StashPopRequest { index: 0 };
    let wire = r#"{"index": 0}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::StashPopRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::StashPopRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous StashPush request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_stash_push_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::StashPushRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::StashPushRequest {
            message: String::from("s1"),
        };
    let wire = r#"{"message": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::StashPushRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::StashPushRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous StashPush request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_stash_push_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::StashPushRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::StashPushRequest {
            message: String::new(),
        };
    let wire = r#"{"message": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::StashPushRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::StashPushRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous Switch request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_switch_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::SwitchRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::SwitchRequest {
            branch: String::from("s1"),
        };
    let wire = r#"{"branch": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::SwitchRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::SwitchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous Switch request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_switch_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::SwitchRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::SwitchRequest {
            branch: String::new(),
        };
    let wire = r#"{"branch": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::SwitchRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::SwitchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous UnstageHunk request>`, fully populated.
#[test]
fn api_v0_endpoints_git_handlers_unstage_hunk_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::UnstageHunkRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::UnstageHunkRequest {
            path: String::from("s1"),
            hunk_id: String::from("s2"),
        };
    let wire = r#"{"path": "s1", "hunkId": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::UnstageHunkRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::UnstageHunkRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/git/handlers.<anonymous UnstageHunk request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_git_handlers_unstage_hunk_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_git_handlers::UnstageHunkRequest =
        crowbar_proto::api_v0_endpoints_git_handlers::UnstageHunkRequest {
            path: String::new(),
            hunk_id: String::new(),
        };
    let wire = r#"{"path": "", "hunkId": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_git_handlers::UnstageHunkRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_git_handlers::UnstageHunkRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/home/handlers.<anonymous CopyFile request>`, fully populated.
#[test]
fn api_v0_endpoints_home_handlers_copy_file_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_home_handlers::CopyFileRequest =
        crowbar_proto::api_v0_endpoints_home_handlers::CopyFileRequest {
            source_path: String::from("s1"),
            dest_path: String::from("s2"),
        };
    let wire = r#"{"sourcePath": "s1", "destPath": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_home_handlers::CopyFileRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_home_handlers::CopyFileRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/home/handlers.<anonymous CopyFile request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_home_handlers_copy_file_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_home_handlers::CopyFileRequest =
        crowbar_proto::api_v0_endpoints_home_handlers::CopyFileRequest {
            source_path: String::new(),
            dest_path: String::new(),
        };
    let wire = r#"{"sourcePath": "", "destPath": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_home_handlers::CopyFileRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_home_handlers::CopyFileRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/home/handlers.<anonymous CreateFile request>`, fully populated.
#[test]
fn api_v0_endpoints_home_handlers_create_file_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_home_handlers::CreateFileRequest =
        crowbar_proto::api_v0_endpoints_home_handlers::CreateFileRequest {
            path: String::from("s1"),
            type_: String::from("s2"),
        };
    let wire = r#"{"path": "s1", "type": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_home_handlers::CreateFileRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_home_handlers::CreateFileRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/home/handlers.<anonymous CreateFile request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_home_handlers_create_file_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_home_handlers::CreateFileRequest =
        crowbar_proto::api_v0_endpoints_home_handlers::CreateFileRequest {
            path: String::new(),
            type_: String::new(),
        };
    let wire = r#"{"path": "", "type": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_home_handlers::CreateFileRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_home_handlers::CreateFileRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/home/handlers.<anonymous DeleteFile request>`, fully populated.
#[test]
fn api_v0_endpoints_home_handlers_delete_file_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_home_handlers::DeleteFileRequest =
        crowbar_proto::api_v0_endpoints_home_handlers::DeleteFileRequest {
            path: String::from("s1"),
        };
    let wire = r#"{"path": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_home_handlers::DeleteFileRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_home_handlers::DeleteFileRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/home/handlers.<anonymous DeleteFile request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_home_handlers_delete_file_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_home_handlers::DeleteFileRequest =
        crowbar_proto::api_v0_endpoints_home_handlers::DeleteFileRequest {
            path: String::new(),
        };
    let wire = r#"{"path": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_home_handlers::DeleteFileRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_home_handlers::DeleteFileRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/home/handlers.<anonymous RenameFile request>`, fully populated.
#[test]
fn api_v0_endpoints_home_handlers_rename_file_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_home_handlers::RenameFileRequest =
        crowbar_proto::api_v0_endpoints_home_handlers::RenameFileRequest {
            path: String::from("s1"),
            new_path: String::from("s2"),
        };
    let wire = r#"{"path": "s1", "newPath": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_home_handlers::RenameFileRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_home_handlers::RenameFileRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/home/handlers.<anonymous RenameFile request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_home_handlers_rename_file_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_home_handlers::RenameFileRequest =
        crowbar_proto::api_v0_endpoints_home_handlers::RenameFileRequest {
            path: String::new(),
            new_path: String::new(),
        };
    let wire = r#"{"path": "", "newPath": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_home_handlers::RenameFileRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_home_handlers::RenameFileRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/home/handlers.<anonymous SaveFileContent request>`, fully populated.
#[test]
fn api_v0_endpoints_home_handlers_save_file_content_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_home_handlers::SaveFileContentRequest =
        crowbar_proto::api_v0_endpoints_home_handlers::SaveFileContentRequest {
            path: String::from("s1"),
            content: String::from("s2"),
            encoding: String::from("s3"),
        };
    let wire = r#"{"path": "s1", "content": "s2", "encoding": "s3"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_home_handlers::SaveFileContentRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_home_handlers::SaveFileContentRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/home/handlers.<anonymous SaveFileContent request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_home_handlers_save_file_content_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_home_handlers::SaveFileContentRequest =
        crowbar_proto::api_v0_endpoints_home_handlers::SaveFileContentRequest {
            path: String::new(),
            content: String::new(),
            encoding: String::new(),
        };
    let wire = r#"{"path": "", "content": "", "encoding": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_home_handlers::SaveFileContentRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_home_handlers::SaveFileContentRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/projects/handlers.importRequest`, fully populated.
#[test]
fn api_v0_endpoints_projects_handlers_import_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_projects_handlers::ImportRequest =
        crowbar_proto::api_v0_endpoints_projects_handlers::ImportRequest {
            name: String::from("s1"),
            path: String::from("s2"),
        };
    let wire = r#"{"name": "s1", "path": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_projects_handlers::ImportRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_projects_handlers::ImportRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/projects/handlers.importRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_projects_handlers_import_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_projects_handlers::ImportRequest =
        crowbar_proto::api_v0_endpoints_projects_handlers::ImportRequest {
            name: String::new(),
            path: String::new(),
        };
    let wire = r#"{"name": "", "path": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_projects_handlers::ImportRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_projects_handlers::ImportRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.BranchEntry`, fully populated.
#[test]
fn api_v0_endpoints_repos_handlers_branch_entry_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::BranchEntry =
        crowbar_proto::api_v0_endpoints_repos_handlers::BranchEntry {
            name: String::from("s1"),
            is_protected: false,
            has_workspace: true,
        };
    let wire = r#"{"name": "s1", "isProtected": false, "hasWorkspace": true}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::BranchEntry =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::BranchEntry =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.BranchEntry` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_repos_handlers_branch_entry_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::BranchEntry =
        crowbar_proto::api_v0_endpoints_repos_handlers::BranchEntry {
            name: String::new(),
            is_protected: false,
            has_workspace: false,
        };
    let wire = r#"{"name": "", "isProtected": false, "hasWorkspace": false}"#;

    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::BranchEntry =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::BranchEntry =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.createRequest`, fully populated.
#[test]
fn api_v0_endpoints_repos_handlers_create_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::CreateRequest =
        crowbar_proto::api_v0_endpoints_repos_handlers::CreateRequest {
            id: String::from("s1"),
            project_id: String::from("s2"),
            name: String::from("s3"),
            path: String::from("s4"),
            default_branch: String::from("s5"),
        };
    let wire =
        r#"{"id": "s1", "projectId": "s2", "name": "s3", "path": "s4", "defaultBranch": "s5"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::CreateRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::CreateRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.createRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_repos_handlers_create_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::CreateRequest =
        crowbar_proto::api_v0_endpoints_repos_handlers::CreateRequest {
            id: String::new(),
            project_id: String::new(),
            name: String::new(),
            path: String::new(),
            default_branch: String::new(),
        };
    let wire = r#"{"id": "", "projectId": "", "name": "", "path": "", "defaultBranch": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::CreateRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::CreateRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.PRLinkDTO`, fully populated.
#[test]
fn api_v0_endpoints_repos_handlers_pr_link_dto_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::PRLinkDTO =
        crowbar_proto::api_v0_endpoints_repos_handlers::PRLinkDTO {
            head: String::from("s1"),
            base: String::from("s2"),
            number: 3,
            status: String::from("s4"),
            url: String::from("s5"),
            title: String::from("s6"),
        };
    let wire =
        r#"{"head": "s1", "base": "s2", "number": 3, "status": "s4", "url": "s5", "title": "s6"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::PRLinkDTO =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::PRLinkDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.PRLinkDTO` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_repos_handlers_pr_link_dto_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::PRLinkDTO =
        crowbar_proto::api_v0_endpoints_repos_handlers::PRLinkDTO {
            head: String::new(),
            base: String::new(),
            number: 0,
            status: String::new(),
            url: String::new(),
            title: String::new(),
        };
    let wire = r#"{"head": "", "base": "", "number": 0, "status": "", "url": "", "title": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::PRLinkDTO =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::PRLinkDTO =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.<anonymous PutIconEmoji request>`, fully populated.
#[test]
fn api_v0_endpoints_repos_handlers_put_icon_emoji_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconEmojiRequest =
        crowbar_proto::api_v0_endpoints_repos_handlers::PutIconEmojiRequest {
            emoji: String::from("s1"),
        };
    let wire = r#"{"emoji": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconEmojiRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconEmojiRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.<anonymous PutIconEmoji request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_repos_handlers_put_icon_emoji_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconEmojiRequest =
        crowbar_proto::api_v0_endpoints_repos_handlers::PutIconEmojiRequest {
            emoji: String::new(),
        };
    let wire = r#"{"emoji": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconEmojiRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconEmojiRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.<anonymous PutIcon request>`, fully populated.
#[test]
fn api_v0_endpoints_repos_handlers_put_icon_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconRequest =
        crowbar_proto::api_v0_endpoints_repos_handlers::PutIconRequest {
            path: String::from("s1"),
        };
    let wire = r#"{"path": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.<anonymous PutIcon request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_repos_handlers_put_icon_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconRequest =
        crowbar_proto::api_v0_endpoints_repos_handlers::PutIconRequest {
            path: String::new(),
        };
    let wire = r#"{"path": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::PutIconRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.renameRequest`, fully populated.
#[test]
fn api_v0_endpoints_repos_handlers_rename_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::RenameRequest =
        crowbar_proto::api_v0_endpoints_repos_handlers::RenameRequest {
            name: String::from("s1"),
        };
    let wire = r#"{"name": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::RenameRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::RenameRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/repos/handlers.renameRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_repos_handlers_rename_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_repos_handlers::RenameRequest =
        crowbar_proto::api_v0_endpoints_repos_handlers::RenameRequest {
            name: String::new(),
        };
    let wire = r#"{"name": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_repos_handlers::RenameRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_repos_handlers::RenameRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/review/handlers.outlineResponse`, fully populated.
#[test]
fn api_v0_endpoints_review_handlers_outline_response_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_review_handlers::OutlineResponse =
        crowbar_proto::api_v0_endpoints_review_handlers::OutlineResponse {
            files: vec![crowbar_proto::domain_git::FileOutline {
                path: String::from("s1"),
                old_path: Some(String::from("s2")),
                hunks: vec![crowbar_proto::domain_git::HunkShape {
                    old_start: 3,
                    old_lines: 4,
                    new_start: 5,
                    new_lines: 6,
                }],
                is_partial: true,
                is_binary: false,
            }],
        };
    let wire = r#"{"files": [{"path": "s1", "oldPath": "s2", "hunks": [{"oldStart": 3, "oldLines": 4, "newStart": 5, "newLines": 6}], "isPartial": true, "isBinary": false}]}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_review_handlers::OutlineResponse =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_review_handlers::OutlineResponse =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/review/handlers.outlineResponse` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_review_handlers_outline_response_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_review_handlers::OutlineResponse =
        crowbar_proto::api_v0_endpoints_review_handlers::OutlineResponse { files: Vec::new() };
    let wire = r#"{"files": null}"#;

    let parsed: crowbar_proto::api_v0_endpoints_review_handlers::OutlineResponse =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"files": []}"#).expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_review_handlers::OutlineResponse =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/review/handlers.reviewFilesResponse`, fully populated.
#[test]
fn api_v0_endpoints_review_handlers_review_files_response_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_review_handlers::ReviewFilesResponse =
        crowbar_proto::api_v0_endpoints_review_handlers::ReviewFilesResponse {
            files: vec![crowbar_proto::domain_git::ReviewFileSummary {
                path: String::from("s1"),
                old_path: Some(String::from("s2")),
                status: crowbar_proto::domain_git::GitFileStatus::Added,
                additions: 3,
                deletions: 4,
                uncommitted: true,
                staged: false,
            }],
        };
    let wire = r#"{"files": [{"path": "s1", "old_path": "s2", "status": "added", "additions": 3, "deletions": 4, "uncommitted": true, "staged": false}]}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_review_handlers::ReviewFilesResponse =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_review_handlers::ReviewFilesResponse =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/review/handlers.reviewFilesResponse` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_review_handlers_review_files_response_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_review_handlers::ReviewFilesResponse =
        crowbar_proto::api_v0_endpoints_review_handlers::ReviewFilesResponse { files: Vec::new() };
    let wire = r#"{"files": null}"#;

    let parsed: crowbar_proto::api_v0_endpoints_review_handlers::ReviewFilesResponse =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"files": []}"#).expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_review_handlers::ReviewFilesResponse =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/review/handlers.searchResponse`, fully populated.
#[test]
fn api_v0_endpoints_review_handlers_search_response_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_review_handlers::SearchResponse =
        crowbar_proto::api_v0_endpoints_review_handlers::SearchResponse {
            hits: vec![crowbar_proto::domain_git::SearchHit {
                path: String::from("s1"),
                side: String::from("s2"),
                line_number: 3,
                preview: String::from("s4"),
            }],
            truncated: true,
        };
    let wire = r#"{"hits": [{"path": "s1", "side": "s2", "lineNumber": 3, "preview": "s4"}], "truncated": true}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_review_handlers::SearchResponse =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_review_handlers::SearchResponse =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/review/handlers.searchResponse` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_review_handlers_search_response_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_review_handlers::SearchResponse =
        crowbar_proto::api_v0_endpoints_review_handlers::SearchResponse {
            hits: Vec::new(),
            truncated: false,
        };
    let wire = r#"{"hits": null, "truncated": false}"#;

    let parsed: crowbar_proto::api_v0_endpoints_review_handlers::SearchResponse =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(r#"{"hits": [], "truncated": false}"#)
        .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_review_handlers::SearchResponse =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/review/handlers.<anonymous SetMergeStrategy request>`, fully populated.
#[test]
fn api_v0_endpoints_review_handlers_set_merge_strategy_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_review_handlers::SetMergeStrategyRequest =
        crowbar_proto::api_v0_endpoints_review_handlers::SetMergeStrategyRequest {
            merge_strategy: crowbar_proto::domain_git::MergeStrategy::Merge,
        };
    let wire = r#"{"mergeStrategy": "merge"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_review_handlers::SetMergeStrategyRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_review_handlers::SetMergeStrategyRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/review/handlers.<anonymous SetMergeStrategy request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_review_handlers_set_merge_strategy_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_review_handlers::SetMergeStrategyRequest =
        crowbar_proto::api_v0_endpoints_review_handlers::SetMergeStrategyRequest {
            merge_strategy: crowbar_proto::domain_git::MergeStrategy::Other(String::new()),
        };
    let wire = r#"{"mergeStrategy": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_review_handlers::SetMergeStrategyRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_review_handlers::SetMergeStrategyRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/search/handlers.<anonymous Replace request>`, fully populated.
#[test]
fn api_v0_endpoints_search_handlers_replace_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_search_handlers::ReplaceRequest =
        crowbar_proto::api_v0_endpoints_search_handlers::ReplaceRequest {
            query: String::from("s1"),
            replacement: String::from("s2"),
            scope: String::from("s3"),
            case_sensitive: false,
            whole_word: true,
            regex: false,
        };
    let wire = r#"{"query": "s1", "replacement": "s2", "scope": "s3", "caseSensitive": false, "wholeWord": true, "regex": false}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_search_handlers::ReplaceRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_search_handlers::ReplaceRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/search/handlers.<anonymous Replace request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_search_handlers_replace_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_search_handlers::ReplaceRequest =
        crowbar_proto::api_v0_endpoints_search_handlers::ReplaceRequest {
            query: String::new(),
            replacement: String::new(),
            scope: String::new(),
            case_sensitive: false,
            whole_word: false,
            regex: false,
        };
    let wire = r#"{"query": "", "replacement": "", "scope": "", "caseSensitive": false, "wholeWord": false, "regex": false}"#;

    let parsed: crowbar_proto::api_v0_endpoints_search_handlers::ReplaceRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_search_handlers::ReplaceRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/search/handlers.<anonymous Search request>`, fully populated.
#[test]
fn api_v0_endpoints_search_handlers_search_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_search_handlers::SearchRequest =
        crowbar_proto::api_v0_endpoints_search_handlers::SearchRequest {
            query: String::from("s1"),
            case_sensitive: false,
            whole_word: true,
            regex: false,
            include: vec![String::from("s5")],
            exclude: vec![String::from("s6")],
        };
    let wire = r#"{"query": "s1", "caseSensitive": false, "wholeWord": true, "regex": false, "include": ["s5"], "exclude": ["s6"]}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_search_handlers::SearchRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_search_handlers::SearchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/search/handlers.<anonymous Search request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_search_handlers_search_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_search_handlers::SearchRequest =
        crowbar_proto::api_v0_endpoints_search_handlers::SearchRequest {
            query: String::new(),
            case_sensitive: false,
            whole_word: false,
            regex: false,
            include: Vec::new(),
            exclude: Vec::new(),
        };
    let wire = r#"{"query": "", "caseSensitive": false, "wholeWord": false, "regex": false, "include": null, "exclude": null}"#;

    let parsed: crowbar_proto::api_v0_endpoints_search_handlers::SearchRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"query": "", "caseSensitive": false, "wholeWord": false, "regex": false, "include": [], "exclude": []}"#)
            .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_search_handlers::SearchRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/system/handlers.CLIStatus`, fully populated.
#[test]
fn api_v0_endpoints_system_handlers_cli_status_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus =
        crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus {
            installed: true,
            authed: false,
        };
    let wire = r#"{"installed": true, "authed": false}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/system/handlers.CLIStatus` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_system_handlers_cli_status_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus =
        crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus {
            installed: false,
            authed: false,
        };
    let wire = r#"{"installed": false, "authed": false}"#;

    let parsed: crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/system/handlers.GitStatus`, fully populated.
#[test]
fn api_v0_endpoints_system_handlers_git_status_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_system_handlers::GitStatus =
        crowbar_proto::api_v0_endpoints_system_handlers::GitStatus {
            installed: true,
            version: Some(String::from("s2")),
        };
    let wire = r#"{"installed": true, "version": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_system_handlers::GitStatus =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_system_handlers::GitStatus =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/system/handlers.GitStatus` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_system_handlers_git_status_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_system_handlers::GitStatus =
        crowbar_proto::api_v0_endpoints_system_handlers::GitStatus {
            installed: false,
            version: None,
        };
    let wire = r#"{"installed": false}"#;

    let parsed: crowbar_proto::api_v0_endpoints_system_handlers::GitStatus =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_system_handlers::GitStatus =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/system/handlers.PerfData`, fully populated.
#[test]
fn api_v0_endpoints_system_handlers_perf_data_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_system_handlers::PerfData =
        crowbar_proto::api_v0_endpoints_system_handlers::PerfData {
            enabled: true,
            samples: vec![crowbar_proto::perf::Sample {
                name: String::from("s2"),
                duration_ms: 3.5,
                at: String::from("2026-07-31T05:00:00.000000004Z"),
            }],
        };
    let wire = r#"{"enabled": true, "samples": [{"name": "s2", "durationMs": 3.5, "at": "2026-07-31T05:00:00.000000004Z"}]}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_system_handlers::PerfData =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_system_handlers::PerfData =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/system/handlers.PerfData` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_system_handlers_perf_data_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_system_handlers::PerfData =
        crowbar_proto::api_v0_endpoints_system_handlers::PerfData {
            enabled: false,
            samples: Vec::new(),
        };
    let wire = r#"{"enabled": false, "samples": null}"#;

    let parsed: crowbar_proto::api_v0_endpoints_system_handlers::PerfData =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(r#"{"enabled": false, "samples": []}"#)
        .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_system_handlers::PerfData =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/system/handlers.PrerequisitesData`, fully populated.
#[test]
fn api_v0_endpoints_system_handlers_prerequisites_data_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_system_handlers::PrerequisitesData =
        crowbar_proto::api_v0_endpoints_system_handlers::PrerequisitesData {
            git: crowbar_proto::api_v0_endpoints_system_handlers::GitStatus {
                installed: true,
                version: Some(String::from("s2")),
            },
            gh: crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus {
                installed: true,
                authed: false,
            },
            glab: crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus {
                installed: true,
                authed: false,
            },
        };
    let wire = r#"{"git": {"installed": true, "version": "s2"}, "gh": {"installed": true, "authed": false}, "glab": {"installed": true, "authed": false}}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_system_handlers::PrerequisitesData =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_system_handlers::PrerequisitesData =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/system/handlers.PrerequisitesData` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_system_handlers_prerequisites_data_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_system_handlers::PrerequisitesData =
        crowbar_proto::api_v0_endpoints_system_handlers::PrerequisitesData {
            git: crowbar_proto::api_v0_endpoints_system_handlers::GitStatus {
                installed: false,
                version: None,
            },
            gh: crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus {
                installed: false,
                authed: false,
            },
            glab: crowbar_proto::api_v0_endpoints_system_handlers::CLIStatus {
                installed: false,
                authed: false,
            },
        };
    let wire = r#"{"git": {"installed": false}, "gh": {"installed": false, "authed": false}, "glab": {"installed": false, "authed": false}}"#;

    let parsed: crowbar_proto::api_v0_endpoints_system_handlers::PrerequisitesData =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_system_handlers::PrerequisitesData =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/terminal/handlers.<anonymous CreateSession request>`, fully populated.
#[test]
fn api_v0_endpoints_terminal_handlers_create_session_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_terminal_handlers::CreateSessionRequest =
        crowbar_proto::api_v0_endpoints_terminal_handlers::CreateSessionRequest {
            profile_id: String::from("s1"),
        };
    let wire = r#"{"profileId": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_terminal_handlers::CreateSessionRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_terminal_handlers::CreateSessionRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/terminal/handlers.<anonymous CreateSession request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_terminal_handlers_create_session_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_terminal_handlers::CreateSessionRequest =
        crowbar_proto::api_v0_endpoints_terminal_handlers::CreateSessionRequest {
            profile_id: String::new(),
        };
    let wire = r#"{"profileId": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_terminal_handlers::CreateSessionRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_terminal_handlers::CreateSessionRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous EditMessage request>`, fully populated.
#[test]
fn api_v0_endpoints_threads_handlers_edit_message_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest =
        crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest {
            body: String::from("s1"),
        };
    let wire = r#"{"body": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous EditMessage request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_threads_handlers_edit_message_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest =
        crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest {
            body: String::new(),
        };
    let wire = r#"{"body": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous EditMessage request>`, fully populated.
#[test]
fn api_v0_endpoints_threads_handlers_edit_message_request2_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest2 =
        crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest2 {
            body: String::from("s1"),
        };
    let wire = r#"{"body": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest2 =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous EditMessage request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_threads_handlers_edit_message_request2_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest2 =
        crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest2 {
            body: String::new(),
        };
    let wire = r#"{"body": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest2 =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::EditMessageRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous OpenThread request>`, fully populated.
#[test]
fn api_v0_endpoints_threads_handlers_open_thread_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest =
        crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest {
            file_path: String::from("s1"),
            line: 2,
            start_line: 3,
            end_line: 4,
            side: crowbar_proto::domain::ReviewSide::Left,
            author: String::from("s5"),
            is_agent: false,
            body: String::from("s7"),
        };
    let wire = r#"{"filePath": "s1", "line": 2, "startLine": 3, "endLine": 4, "side": "left", "author": "s5", "isAgent": false, "body": "s7"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous OpenThread request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_threads_handlers_open_thread_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest =
        crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest {
            file_path: String::new(),
            line: 0,
            start_line: 0,
            end_line: 0,
            side: crowbar_proto::domain::ReviewSide::Other(String::new()),
            author: String::new(),
            is_agent: false,
            body: String::new(),
        };
    let wire = r#"{"filePath": "", "line": 0, "startLine": 0, "endLine": 0, "side": "", "author": "", "isAgent": false, "body": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous OpenThread request>`, fully populated.
#[test]
fn api_v0_endpoints_threads_handlers_open_thread_request2_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest2 =
        crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest2 {
            file_path: String::from("s1"),
            line: 2,
            start_line: 3,
            end_line: 4,
            side: crowbar_proto::domain::ReviewSide::Left,
            author: String::from("s5"),
            is_agent: false,
            body: String::from("s7"),
        };
    let wire = r#"{"filePath": "s1", "line": 2, "startLine": 3, "endLine": 4, "side": "left", "author": "s5", "isAgent": false, "body": "s7"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest2 =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous OpenThread request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_threads_handlers_open_thread_request2_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest2 =
        crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest2 {
            file_path: String::new(),
            line: 0,
            start_line: 0,
            end_line: 0,
            side: crowbar_proto::domain::ReviewSide::Other(String::new()),
            author: String::new(),
            is_agent: false,
            body: String::new(),
        };
    let wire = r#"{"filePath": "", "line": 0, "startLine": 0, "endLine": 0, "side": "", "author": "", "isAgent": false, "body": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest2 =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::OpenThreadRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous Reply request>`, fully populated.
#[test]
fn api_v0_endpoints_threads_handlers_reply_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest =
        crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest {
            author: String::from("s1"),
            is_agent: false,
            body: String::from("s3"),
        };
    let wire = r#"{"author": "s1", "isAgent": false, "body": "s3"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous Reply request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_threads_handlers_reply_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest =
        crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest {
            author: String::new(),
            is_agent: false,
            body: String::new(),
        };
    let wire = r#"{"author": "", "isAgent": false, "body": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous Reply request>`, fully populated.
#[test]
fn api_v0_endpoints_threads_handlers_reply_request2_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest2 =
        crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest2 {
            author: String::from("s1"),
            is_agent: false,
            body: String::from("s3"),
        };
    let wire = r#"{"author": "s1", "isAgent": false, "body": "s3"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest2 =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous Reply request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_threads_handlers_reply_request2_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest2 =
        crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest2 {
            author: String::new(),
            is_agent: false,
            body: String::new(),
        };
    let wire = r#"{"author": "", "isAgent": false, "body": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest2 =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::ReplyRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous SetResolved request>`, fully populated.
#[test]
fn api_v0_endpoints_threads_handlers_set_resolved_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest =
        crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest { is_resolved: true };
    let wire = r#"{"isResolved": true}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous SetResolved request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_threads_handlers_set_resolved_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest =
        crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest { is_resolved: false };
    let wire = r#"{"isResolved": false}"#;

    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous SetResolved request>`, fully populated.
#[test]
fn api_v0_endpoints_threads_handlers_set_resolved_request2_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest2 =
        crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest2 { is_resolved: true };
    let wire = r#"{"isResolved": true}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest2 =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/threads/handlers.<anonymous SetResolved request>` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_threads_handlers_set_resolved_request2_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest2 =
        crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest2 {
            is_resolved: false,
        };
    let wire = r#"{"isResolved": false}"#;

    let parsed: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest2 =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_threads_handlers::SetResolvedRequest2 =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/workspaces/handlers.createRequest`, fully populated.
#[test]
fn api_v0_endpoints_workspaces_handlers_create_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_workspaces_handlers::CreateRequest =
        crowbar_proto::api_v0_endpoints_workspaces_handlers::CreateRequest {
            branch: String::from("s1"),
            parent_id: String::from("s2"),
        };
    let wire = r#"{"branch": "s1", "parentId": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_workspaces_handlers::CreateRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_workspaces_handlers::CreateRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/workspaces/handlers.createRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_workspaces_handlers_create_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_workspaces_handlers::CreateRequest =
        crowbar_proto::api_v0_endpoints_workspaces_handlers::CreateRequest {
            branch: String::new(),
            parent_id: String::new(),
        };
    let wire = r#"{"branch": "", "parentId": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_workspaces_handlers::CreateRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_workspaces_handlers::CreateRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/workspaces/handlers.importRequest`, fully populated.
#[test]
fn api_v0_endpoints_workspaces_handlers_import_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_workspaces_handlers::ImportRequest =
        crowbar_proto::api_v0_endpoints_workspaces_handlers::ImportRequest {
            branches: vec![String::from("s1")],
        };
    let wire = r#"{"branches": ["s1"]}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_workspaces_handlers::ImportRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_workspaces_handlers::ImportRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/workspaces/handlers.importRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_workspaces_handlers_import_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_workspaces_handlers::ImportRequest =
        crowbar_proto::api_v0_endpoints_workspaces_handlers::ImportRequest {
            branches: Vec::new(),
        };
    let wire = r#"{"branches": null}"#;

    let parsed: crowbar_proto::api_v0_endpoints_workspaces_handlers::ImportRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"branches": []}"#).expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_workspaces_handlers::ImportRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/workspaces/handlers.mergeRequest`, fully populated.
#[test]
fn api_v0_endpoints_workspaces_handlers_merge_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_workspaces_handlers::MergeRequest =
        crowbar_proto::api_v0_endpoints_workspaces_handlers::MergeRequest {
            strategy: crowbar_proto::domain_git::MergeStrategy::Merge,
            delete_source: true,
        };
    let wire = r#"{"strategy": "merge", "deleteSource": true}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_workspaces_handlers::MergeRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_workspaces_handlers::MergeRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/workspaces/handlers.mergeRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_workspaces_handlers_merge_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_workspaces_handlers::MergeRequest =
        crowbar_proto::api_v0_endpoints_workspaces_handlers::MergeRequest {
            strategy: crowbar_proto::domain_git::MergeStrategy::Other(String::new()),
            delete_source: false,
        };
    let wire = r#"{"strategy": "", "deleteSource": false}"#;

    let parsed: crowbar_proto::api_v0_endpoints_workspaces_handlers::MergeRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_workspaces_handlers::MergeRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/workspaces/handlers.renameRequest`, fully populated.
#[test]
fn api_v0_endpoints_workspaces_handlers_rename_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_workspaces_handlers::RenameRequest =
        crowbar_proto::api_v0_endpoints_workspaces_handlers::RenameRequest {
            branch: String::from("s1"),
        };
    let wire = r#"{"branch": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_workspaces_handlers::RenameRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_workspaces_handlers::RenameRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/workspaces/handlers.renameRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_workspaces_handlers_rename_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_workspaces_handlers::RenameRequest =
        crowbar_proto::api_v0_endpoints_workspaces_handlers::RenameRequest {
            branch: String::new(),
        };
    let wire = r#"{"branch": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_workspaces_handlers::RenameRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_workspaces_handlers::RenameRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/workspaces/handlers.reparentRequest`, fully populated.
#[test]
fn api_v0_endpoints_workspaces_handlers_reparent_request_wire_shape() {
    let value: crowbar_proto::api_v0_endpoints_workspaces_handlers::ReparentRequest =
        crowbar_proto::api_v0_endpoints_workspaces_handlers::ReparentRequest {
            new_parent_id: String::from("s1"),
        };
    let wire = r#"{"newParentId": "s1"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::api_v0_endpoints_workspaces_handlers::ReparentRequest =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_workspaces_handlers::ReparentRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/api/v0/endpoints/workspaces/handlers.reparentRequest` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn api_v0_endpoints_workspaces_handlers_reparent_request_zero_values() {
    let value: crowbar_proto::api_v0_endpoints_workspaces_handlers::ReparentRequest =
        crowbar_proto::api_v0_endpoints_workspaces_handlers::ReparentRequest {
            new_parent_id: String::new(),
        };
    let wire = r#"{"newParentId": ""}"#;

    let parsed: crowbar_proto::api_v0_endpoints_workspaces_handlers::ReparentRequest =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::api_v0_endpoints_workspaces_handlers::ReparentRequest =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain.ChatConversation`, fully populated.
#[test]
fn domain_chat_conversation_wire_shape() {
    let value: crowbar_proto::domain::ChatConversation = crowbar_proto::domain::ChatConversation {
        chat_id: String::from("s1"),
        provider_id: String::from("s2"),
        session_id: String::from("s3"),
        first_seen_at: String::from("2026-07-31T05:00:00.000000004Z"),
    };
    let wire = r#"{"chatId": "s1", "providerId": "s2", "sessionId": "s3", "firstSeenAt": "2026-07-31T05:00:00.000000004Z"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain::ChatConversation =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain::ChatConversation =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain.ChatConversation` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_chat_conversation_zero_values() {
    let value: crowbar_proto::domain::ChatConversation = crowbar_proto::domain::ChatConversation {
        chat_id: String::new(),
        provider_id: String::new(),
        session_id: String::new(),
        first_seen_at: String::new(),
    };
    let wire = r#"{"chatId": "", "providerId": "", "sessionId": "", "firstSeenAt": ""}"#;

    let parsed: crowbar_proto::domain::ChatConversation =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain::ChatConversation =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain.FileContent`, fully populated.
#[test]
fn domain_file_content_wire_shape() {
    let value: crowbar_proto::domain::FileContent = crowbar_proto::domain::FileContent {
        content: String::from("s1"),
        encoding: Some(String::from("s2")),
    };
    let wire = r#"{"content": "s1", "encoding": "s2"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain::FileContent =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain::FileContent = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain.FileContent` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_file_content_zero_values() {
    let value: crowbar_proto::domain::FileContent = crowbar_proto::domain::FileContent {
        content: String::new(),
        encoding: None,
    };
    let wire = r#"{"content": ""}"#;

    let parsed: crowbar_proto::domain::FileContent =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain::FileContent = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain.FileNode`, fully populated.
#[test]
fn domain_file_node_wire_shape() {
    let value: crowbar_proto::domain::FileNode = crowbar_proto::domain::FileNode {
        name: String::from("s1"),
        path: String::from("s2"),
        type_: crowbar_proto::domain::FileNodeType::Directory,
        children: Some(vec![crowbar_proto::domain::FileNode {
            name: String::new(),
            path: String::new(),
            type_: crowbar_proto::domain::FileNodeType::Other(String::new()),
            children: None,
            git_status: None,
        }]),
        git_status: Some(crowbar_proto::domain::FileNodeGitStatus::Added),
    };
    let wire = r#"{"name": "s1", "path": "s2", "type": "directory", "children": [{"name": "", "path": "", "type": ""}], "gitStatus": "added"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain::FileNode =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain::FileNode = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain.FileNode` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_file_node_zero_values() {
    let value: crowbar_proto::domain::FileNode = crowbar_proto::domain::FileNode {
        name: String::new(),
        path: String::new(),
        type_: crowbar_proto::domain::FileNodeType::Other(String::new()),
        children: None,
        git_status: None,
    };
    let wire = r#"{"name": "", "path": "", "type": ""}"#;

    let parsed: crowbar_proto::domain::FileNode =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain::FileNode = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// Every constant of `github.com/char2cs/crowbar/api/internal/domain.FileNodeGitStatus`, by its exact wire value, plus the
/// open-set fallback a Go named string type requires.
#[test]
fn domain_file_node_git_status_variants() {
    let cases: [(&str, crowbar_proto::domain::FileNodeGitStatus); 5] = [
        ("added", crowbar_proto::domain::FileNodeGitStatus::Added),
        ("deleted", crowbar_proto::domain::FileNodeGitStatus::Deleted),
        (
            "modified",
            crowbar_proto::domain::FileNodeGitStatus::Modified,
        ),
        ("renamed", crowbar_proto::domain::FileNodeGitStatus::Renamed),
        (
            "untracked",
            crowbar_proto::domain::FileNodeGitStatus::Untracked,
        ),
    ];
    for (wire, variant) in cases {
        let json = serde_json::to_string(wire).expect("quote the wire value");
        let parsed: crowbar_proto::domain::FileNodeGitStatus =
            serde_json::from_str(&json).expect("deserialise a declared constant");
        assert_eq!(parsed, variant);
        assert_eq!(serde_json::to_string(&variant).expect("serialise"), json);

        // A declared value must not fall through to the untagged variant.
        // This is the assertion that fails if `Other` is ever ordered
        // before the named variants: untagged matching is first-wins.
        assert_ne!(
            parsed,
            crowbar_proto::domain::FileNodeGitStatus::Other(wire.to_owned())
        );

        assert_eq!(variant.clone(), variant);
        assert!(!format!("{variant:?}").is_empty());
    }

    // Go's zero value is a legal value that no constant names, which is
    // the whole reason the untagged variant exists.
    let zero: crowbar_proto::domain::FileNodeGitStatus =
        serde_json::from_str("\"\"").expect("deserialise the zero value");
    assert_eq!(
        zero,
        crowbar_proto::domain::FileNodeGitStatus::Other(String::new())
    );
    assert_eq!(serde_json::to_string(&zero).expect("serialise"), "\"\"");

    // And a value added on the Go side after this was generated must widen
    // the set rather than fail the whole response.
    let unknown: crowbar_proto::domain::FileNodeGitStatus =
        serde_json::from_str("\"a-value-no-constant-declares\"")
            .expect("deserialise an undeclared value");
    assert_eq!(
        unknown,
        crowbar_proto::domain::FileNodeGitStatus::Other("a-value-no-constant-declares".to_owned())
    );
    assert_eq!(
        serde_json::to_string(&unknown).expect("serialise"),
        "\"a-value-no-constant-declares\"",
    );
    assert_eq!(unknown.clone(), unknown);
    assert!(!format!("{unknown:?}").is_empty());
}

/// Every constant of `github.com/char2cs/crowbar/api/internal/domain.FileNodeType`, by its exact wire value, plus the
/// open-set fallback a Go named string type requires.
#[test]
fn domain_file_node_type_variants() {
    let cases: [(&str, crowbar_proto::domain::FileNodeType); 2] = [
        ("directory", crowbar_proto::domain::FileNodeType::Directory),
        ("file", crowbar_proto::domain::FileNodeType::File),
    ];
    for (wire, variant) in cases {
        let json = serde_json::to_string(wire).expect("quote the wire value");
        let parsed: crowbar_proto::domain::FileNodeType =
            serde_json::from_str(&json).expect("deserialise a declared constant");
        assert_eq!(parsed, variant);
        assert_eq!(serde_json::to_string(&variant).expect("serialise"), json);

        // A declared value must not fall through to the untagged variant.
        // This is the assertion that fails if `Other` is ever ordered
        // before the named variants: untagged matching is first-wins.
        assert_ne!(
            parsed,
            crowbar_proto::domain::FileNodeType::Other(wire.to_owned())
        );

        assert_eq!(variant.clone(), variant);
        assert!(!format!("{variant:?}").is_empty());
    }

    // Go's zero value is a legal value that no constant names, which is
    // the whole reason the untagged variant exists.
    let zero: crowbar_proto::domain::FileNodeType =
        serde_json::from_str("\"\"").expect("deserialise the zero value");
    assert_eq!(
        zero,
        crowbar_proto::domain::FileNodeType::Other(String::new())
    );
    assert_eq!(serde_json::to_string(&zero).expect("serialise"), "\"\"");

    // And a value added on the Go side after this was generated must widen
    // the set rather than fail the whole response.
    let unknown: crowbar_proto::domain::FileNodeType =
        serde_json::from_str("\"a-value-no-constant-declares\"")
            .expect("deserialise an undeclared value");
    assert_eq!(
        unknown,
        crowbar_proto::domain::FileNodeType::Other("a-value-no-constant-declares".to_owned())
    );
    assert_eq!(
        serde_json::to_string(&unknown).expect("serialise"),
        "\"a-value-no-constant-declares\"",
    );
    assert_eq!(unknown.clone(), unknown);
    assert!(!format!("{unknown:?}").is_empty());
}

/// Every constant of `github.com/char2cs/crowbar/api/internal/domain.ReviewSide`, by its exact wire value, plus the
/// open-set fallback a Go named string type requires.
#[test]
fn domain_review_side_variants() {
    let cases: [(&str, crowbar_proto::domain::ReviewSide); 2] = [
        ("left", crowbar_proto::domain::ReviewSide::Left),
        ("right", crowbar_proto::domain::ReviewSide::Right),
    ];
    for (wire, variant) in cases {
        let json = serde_json::to_string(wire).expect("quote the wire value");
        let parsed: crowbar_proto::domain::ReviewSide =
            serde_json::from_str(&json).expect("deserialise a declared constant");
        assert_eq!(parsed, variant);
        assert_eq!(serde_json::to_string(&variant).expect("serialise"), json);

        // A declared value must not fall through to the untagged variant.
        // This is the assertion that fails if `Other` is ever ordered
        // before the named variants: untagged matching is first-wins.
        assert_ne!(
            parsed,
            crowbar_proto::domain::ReviewSide::Other(wire.to_owned())
        );

        assert_eq!(variant.clone(), variant);
        assert!(!format!("{variant:?}").is_empty());
    }

    // Go's zero value is a legal value that no constant names, which is
    // the whole reason the untagged variant exists.
    let zero: crowbar_proto::domain::ReviewSide =
        serde_json::from_str("\"\"").expect("deserialise the zero value");
    assert_eq!(
        zero,
        crowbar_proto::domain::ReviewSide::Other(String::new())
    );
    assert_eq!(serde_json::to_string(&zero).expect("serialise"), "\"\"");

    // And a value added on the Go side after this was generated must widen
    // the set rather than fail the whole response.
    let unknown: crowbar_proto::domain::ReviewSide =
        serde_json::from_str("\"a-value-no-constant-declares\"")
            .expect("deserialise an undeclared value");
    assert_eq!(
        unknown,
        crowbar_proto::domain::ReviewSide::Other("a-value-no-constant-declares".to_owned())
    );
    assert_eq!(
        serde_json::to_string(&unknown).expect("serialise"),
        "\"a-value-no-constant-declares\"",
    );
    assert_eq!(unknown.clone(), unknown);
    assert!(!format!("{unknown:?}").is_empty());
}

/// Every constant of `github.com/char2cs/crowbar/api/internal/domain.ReviewThreadStatus`, by its exact wire value, plus the
/// open-set fallback a Go named string type requires.
#[test]
fn domain_review_thread_status_variants() {
    let cases: [(&str, crowbar_proto::domain::ReviewThreadStatus); 2] = [
        ("open", crowbar_proto::domain::ReviewThreadStatus::Open),
        (
            "resolved",
            crowbar_proto::domain::ReviewThreadStatus::Resolved,
        ),
    ];
    for (wire, variant) in cases {
        let json = serde_json::to_string(wire).expect("quote the wire value");
        let parsed: crowbar_proto::domain::ReviewThreadStatus =
            serde_json::from_str(&json).expect("deserialise a declared constant");
        assert_eq!(parsed, variant);
        assert_eq!(serde_json::to_string(&variant).expect("serialise"), json);

        // A declared value must not fall through to the untagged variant.
        // This is the assertion that fails if `Other` is ever ordered
        // before the named variants: untagged matching is first-wins.
        assert_ne!(
            parsed,
            crowbar_proto::domain::ReviewThreadStatus::Other(wire.to_owned())
        );

        assert_eq!(variant.clone(), variant);
        assert!(!format!("{variant:?}").is_empty());
    }

    // Go's zero value is a legal value that no constant names, which is
    // the whole reason the untagged variant exists.
    let zero: crowbar_proto::domain::ReviewThreadStatus =
        serde_json::from_str("\"\"").expect("deserialise the zero value");
    assert_eq!(
        zero,
        crowbar_proto::domain::ReviewThreadStatus::Other(String::new())
    );
    assert_eq!(serde_json::to_string(&zero).expect("serialise"), "\"\"");

    // And a value added on the Go side after this was generated must widen
    // the set rather than fail the whole response.
    let unknown: crowbar_proto::domain::ReviewThreadStatus =
        serde_json::from_str("\"a-value-no-constant-declares\"")
            .expect("deserialise an undeclared value");
    assert_eq!(
        unknown,
        crowbar_proto::domain::ReviewThreadStatus::Other("a-value-no-constant-declares".to_owned())
    );
    assert_eq!(
        serde_json::to_string(&unknown).expect("serialise"),
        "\"a-value-no-constant-declares\"",
    );
    assert_eq!(unknown.clone(), unknown);
    assert!(!format!("{unknown:?}").is_empty());
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain.TerminalProfile`, fully populated.
#[test]
fn domain_terminal_profile_wire_shape() {
    let value: crowbar_proto::domain::TerminalProfile = crowbar_proto::domain::TerminalProfile {
        id: String::from("s1"),
        name: String::from("s2"),
        shell: Some(String::from("s3")),
        startup_directory: Some(String::from("s4")),
        startup_commands: vec![String::from("s5")],
        icon: Some(String::from("s6")),
        color: Some(String::from("s7")),
    };
    let wire = r#"{"id": "s1", "name": "s2", "shell": "s3", "startupDirectory": "s4", "startupCommands": ["s5"], "icon": "s6", "color": "s7"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain::TerminalProfile =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain::TerminalProfile =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain.TerminalProfile` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_terminal_profile_zero_values() {
    let value: crowbar_proto::domain::TerminalProfile = crowbar_proto::domain::TerminalProfile {
        id: String::new(),
        name: String::new(),
        shell: None,
        startup_directory: None,
        startup_commands: Vec::new(),
        icon: None,
        color: None,
    };
    let wire = r#"{"id": "", "name": "", "startupCommands": null}"#;

    let parsed: crowbar_proto::domain::TerminalProfile =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"id": "", "name": "", "startupCommands": []}"#)
            .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain::TerminalProfile =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// Every constant of `github.com/char2cs/crowbar/api/internal/domain.WorkspaceKind`, by its exact wire value, plus the
/// open-set fallback a Go named string type requires.
#[test]
fn domain_workspace_kind_variants() {
    let cases: [(&str, crowbar_proto::domain::WorkspaceKind); 2] = [
        ("git", crowbar_proto::domain::WorkspaceKind::Git),
        ("home", crowbar_proto::domain::WorkspaceKind::Home),
    ];
    for (wire, variant) in cases {
        let json = serde_json::to_string(wire).expect("quote the wire value");
        let parsed: crowbar_proto::domain::WorkspaceKind =
            serde_json::from_str(&json).expect("deserialise a declared constant");
        assert_eq!(parsed, variant);
        assert_eq!(serde_json::to_string(&variant).expect("serialise"), json);

        // A declared value must not fall through to the untagged variant.
        // This is the assertion that fails if `Other` is ever ordered
        // before the named variants: untagged matching is first-wins.
        assert_ne!(
            parsed,
            crowbar_proto::domain::WorkspaceKind::Other(wire.to_owned())
        );

        assert_eq!(variant.clone(), variant);
        assert!(!format!("{variant:?}").is_empty());
    }

    // Go's zero value is a legal value that no constant names, which is
    // the whole reason the untagged variant exists.
    let zero: crowbar_proto::domain::WorkspaceKind =
        serde_json::from_str("\"\"").expect("deserialise the zero value");
    assert_eq!(
        zero,
        crowbar_proto::domain::WorkspaceKind::Other(String::new())
    );
    assert_eq!(serde_json::to_string(&zero).expect("serialise"), "\"\"");

    // And a value added on the Go side after this was generated must widen
    // the set rather than fail the whole response.
    let unknown: crowbar_proto::domain::WorkspaceKind =
        serde_json::from_str("\"a-value-no-constant-declares\"")
            .expect("deserialise an undeclared value");
    assert_eq!(
        unknown,
        crowbar_proto::domain::WorkspaceKind::Other("a-value-no-constant-declares".to_owned())
    );
    assert_eq!(
        serde_json::to_string(&unknown).expect("serialise"),
        "\"a-value-no-constant-declares\"",
    );
    assert_eq!(unknown.clone(), unknown);
    assert!(!format!("{unknown:?}").is_empty());
}

/// Every constant of `github.com/char2cs/crowbar/api/internal/domain.WorkspaceStatus`, by its exact wire value, plus the
/// open-set fallback a Go named string type requires.
#[test]
fn domain_workspace_status_variants() {
    let cases: [(&str, crowbar_proto::domain::WorkspaceStatus); 7] = [
        ("deleted", crowbar_proto::domain::WorkspaceStatus::Deleted),
        ("locked", crowbar_proto::domain::WorkspaceStatus::Locked),
        ("new", crowbar_proto::domain::WorkspaceStatus::New),
        (
            "pr-closed",
            crowbar_proto::domain::WorkspaceStatus::PrClosed,
        ),
        (
            "pr-conflicts",
            crowbar_proto::domain::WorkspaceStatus::PrConflicts,
        ),
        (
            "pr-merged",
            crowbar_proto::domain::WorkspaceStatus::PrMerged,
        ),
        ("pr-open", crowbar_proto::domain::WorkspaceStatus::PrOpen),
    ];
    for (wire, variant) in cases {
        let json = serde_json::to_string(wire).expect("quote the wire value");
        let parsed: crowbar_proto::domain::WorkspaceStatus =
            serde_json::from_str(&json).expect("deserialise a declared constant");
        assert_eq!(parsed, variant);
        assert_eq!(serde_json::to_string(&variant).expect("serialise"), json);

        // A declared value must not fall through to the untagged variant.
        // This is the assertion that fails if `Other` is ever ordered
        // before the named variants: untagged matching is first-wins.
        assert_ne!(
            parsed,
            crowbar_proto::domain::WorkspaceStatus::Other(wire.to_owned())
        );

        assert_eq!(variant.clone(), variant);
        assert!(!format!("{variant:?}").is_empty());
    }

    // Go's zero value is a legal value that no constant names, which is
    // the whole reason the untagged variant exists.
    let zero: crowbar_proto::domain::WorkspaceStatus =
        serde_json::from_str("\"\"").expect("deserialise the zero value");
    assert_eq!(
        zero,
        crowbar_proto::domain::WorkspaceStatus::Other(String::new())
    );
    assert_eq!(serde_json::to_string(&zero).expect("serialise"), "\"\"");

    // And a value added on the Go side after this was generated must widen
    // the set rather than fail the whole response.
    let unknown: crowbar_proto::domain::WorkspaceStatus =
        serde_json::from_str("\"a-value-no-constant-declares\"")
            .expect("deserialise an undeclared value");
    assert_eq!(
        unknown,
        crowbar_proto::domain::WorkspaceStatus::Other("a-value-no-constant-declares".to_owned())
    );
    assert_eq!(
        serde_json::to_string(&unknown).expect("serialise"),
        "\"a-value-no-constant-declares\"",
    );
    assert_eq!(unknown.clone(), unknown);
    assert!(!format!("{unknown:?}").is_empty());
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.BlameEntry`, fully populated.
#[test]
fn domain_git_blame_entry_wire_shape() {
    let value: crowbar_proto::domain_git::BlameEntry = crowbar_proto::domain_git::BlameEntry {
        line_number: 1,
        commit_hash: String::from("s2"),
        author: String::from("s3"),
        email: String::from("s4"),
        date: String::from("2026-07-31T05:00:00.000000005Z"),
        commit_message: String::from("s6"),
    };
    let wire = r#"{"lineNumber": 1, "commitHash": "s2", "author": "s3", "email": "s4", "date": "2026-07-31T05:00:00.000000005Z", "commitMessage": "s6"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::BlameEntry =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::BlameEntry =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.BlameEntry` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_blame_entry_zero_values() {
    let value: crowbar_proto::domain_git::BlameEntry = crowbar_proto::domain_git::BlameEntry {
        line_number: 0,
        commit_hash: String::new(),
        author: String::new(),
        email: String::new(),
        date: String::new(),
        commit_message: String::new(),
    };
    let wire = r#"{"lineNumber": 0, "commitHash": "", "author": "", "email": "", "date": "", "commitMessage": ""}"#;

    let parsed: crowbar_proto::domain_git::BlameEntry =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::BlameEntry =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.Branch`, fully populated.
#[test]
fn domain_git_branch_wire_shape() {
    let value: crowbar_proto::domain_git::Branch = crowbar_proto::domain_git::Branch {
        name: String::from("s1"),
        is_current: false,
        is_remote: true,
        ahead: Some(4),
        behind: Some(5),
        last_commit_date: Some(String::from("2026-07-31T05:00:00.000000006Z")),
    };
    let wire = r#"{"name": "s1", "isCurrent": false, "isRemote": true, "ahead": 4, "behind": 5, "lastCommitDate": "2026-07-31T05:00:00.000000006Z"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::Branch =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::Branch = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.Branch` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_branch_zero_values() {
    let value: crowbar_proto::domain_git::Branch = crowbar_proto::domain_git::Branch {
        name: String::new(),
        is_current: false,
        is_remote: false,
        ahead: None,
        behind: None,
        last_commit_date: None,
    };
    let wire = r#"{"name": "", "isCurrent": false, "isRemote": false}"#;

    let parsed: crowbar_proto::domain_git::Branch =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::Branch = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.Commit`, fully populated.
#[test]
fn domain_git_commit_wire_shape() {
    let value: crowbar_proto::domain_git::Commit = crowbar_proto::domain_git::Commit {
        hash: String::from("s1"),
        short_hash: String::from("s2"),
        message: String::from("s3"),
        description: Some(String::from("s4")),
        author: String::from("s5"),
        email: Some(String::from("s6")),
        date: String::from("2026-07-31T05:00:00.000000007Z"),
    };
    let wire = r#"{"hash": "s1", "shortHash": "s2", "message": "s3", "description": "s4", "author": "s5", "email": "s6", "date": "2026-07-31T05:00:00.000000007Z"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::Commit =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::Commit = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.Commit` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_commit_zero_values() {
    let value: crowbar_proto::domain_git::Commit = crowbar_proto::domain_git::Commit {
        hash: String::new(),
        short_hash: String::new(),
        message: String::new(),
        description: None,
        author: String::new(),
        email: None,
        date: String::new(),
    };
    let wire = r#"{"hash": "", "shortHash": "", "message": "", "author": "", "date": ""}"#;

    let parsed: crowbar_proto::domain_git::Commit =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::Commit = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.ConflictHunk`, fully populated.
#[test]
fn domain_git_conflict_hunk_wire_shape() {
    let value: crowbar_proto::domain_git::ConflictHunk = crowbar_proto::domain_git::ConflictHunk {
        id: String::from("s1"),
        start_line: 2,
        end_line: 3,
        ours: String::from("s4"),
        theirs: String::from("s5"),
        base: Some(String::from("s6")),
        resolution: crowbar_proto::domain_git::ConflictResolution::Both,
        resolved_content: Some(String::from("s7")),
    };
    let wire = r#"{"id": "s1", "startLine": 2, "endLine": 3, "ours": "s4", "theirs": "s5", "base": "s6", "resolution": "both", "resolvedContent": "s7"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::ConflictHunk =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::ConflictHunk =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.ConflictHunk` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_conflict_hunk_zero_values() {
    let value: crowbar_proto::domain_git::ConflictHunk = crowbar_proto::domain_git::ConflictHunk {
        id: String::new(),
        start_line: 0,
        end_line: 0,
        ours: String::new(),
        theirs: String::new(),
        base: None,
        resolution: crowbar_proto::domain_git::ConflictResolution::Other(String::new()),
        resolved_content: None,
    };
    let wire =
        r#"{"id": "", "startLine": 0, "endLine": 0, "ours": "", "theirs": "", "resolution": ""}"#;

    let parsed: crowbar_proto::domain_git::ConflictHunk =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::ConflictHunk =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// Every constant of `github.com/char2cs/crowbar/api/internal/domain/git.ConflictResolution`, by its exact wire value, plus the
/// open-set fallback a Go named string type requires.
#[test]
fn domain_git_conflict_resolution_variants() {
    let cases: [(&str, crowbar_proto::domain_git::ConflictResolution); 5] = [
        ("both", crowbar_proto::domain_git::ConflictResolution::Both),
        (
            "custom",
            crowbar_proto::domain_git::ConflictResolution::Custom,
        ),
        ("ours", crowbar_proto::domain_git::ConflictResolution::Ours),
        (
            "theirs",
            crowbar_proto::domain_git::ConflictResolution::Theirs,
        ),
        (
            "unresolved",
            crowbar_proto::domain_git::ConflictResolution::Unresolved,
        ),
    ];
    for (wire, variant) in cases {
        let json = serde_json::to_string(wire).expect("quote the wire value");
        let parsed: crowbar_proto::domain_git::ConflictResolution =
            serde_json::from_str(&json).expect("deserialise a declared constant");
        assert_eq!(parsed, variant);
        assert_eq!(serde_json::to_string(&variant).expect("serialise"), json);

        // A declared value must not fall through to the untagged variant.
        // This is the assertion that fails if `Other` is ever ordered
        // before the named variants: untagged matching is first-wins.
        assert_ne!(
            parsed,
            crowbar_proto::domain_git::ConflictResolution::Other(wire.to_owned())
        );

        assert_eq!(variant.clone(), variant);
        assert!(!format!("{variant:?}").is_empty());
    }

    // Go's zero value is a legal value that no constant names, which is
    // the whole reason the untagged variant exists.
    let zero: crowbar_proto::domain_git::ConflictResolution =
        serde_json::from_str("\"\"").expect("deserialise the zero value");
    assert_eq!(
        zero,
        crowbar_proto::domain_git::ConflictResolution::Other(String::new())
    );
    assert_eq!(serde_json::to_string(&zero).expect("serialise"), "\"\"");

    // And a value added on the Go side after this was generated must widen
    // the set rather than fail the whole response.
    let unknown: crowbar_proto::domain_git::ConflictResolution =
        serde_json::from_str("\"a-value-no-constant-declares\"")
            .expect("deserialise an undeclared value");
    assert_eq!(
        unknown,
        crowbar_proto::domain_git::ConflictResolution::Other(
            "a-value-no-constant-declares".to_owned()
        )
    );
    assert_eq!(
        serde_json::to_string(&unknown).expect("serialise"),
        "\"a-value-no-constant-declares\"",
    );
    assert_eq!(unknown.clone(), unknown);
    assert!(!format!("{unknown:?}").is_empty());
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.DiffLine`, fully populated.
#[test]
fn domain_git_diff_line_wire_shape() {
    let value: crowbar_proto::domain_git::DiffLine = crowbar_proto::domain_git::DiffLine {
        line_type: crowbar_proto::domain_git::DiffLineType::Added,
        content: String::from("s1"),
        old_line_number: Some(2),
        new_line_number: Some(3),
        hunk_id: Some(String::from("s4")),
    };
    let wire = r#"{"line_type": "added", "content": "s1", "old_line_number": 2, "new_line_number": 3, "hunkId": "s4"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::DiffLine =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::DiffLine =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.DiffLine` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_diff_line_zero_values() {
    let value: crowbar_proto::domain_git::DiffLine = crowbar_proto::domain_git::DiffLine {
        line_type: crowbar_proto::domain_git::DiffLineType::Other(String::new()),
        content: String::new(),
        old_line_number: None,
        new_line_number: None,
        hunk_id: None,
    };
    let wire = r#"{"line_type": "", "content": ""}"#;

    let parsed: crowbar_proto::domain_git::DiffLine =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::DiffLine =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// Every constant of `github.com/char2cs/crowbar/api/internal/domain/git.DiffLineType`, by its exact wire value, plus the
/// open-set fallback a Go named string type requires.
#[test]
fn domain_git_diff_line_type_variants() {
    let cases: [(&str, crowbar_proto::domain_git::DiffLineType); 4] = [
        ("added", crowbar_proto::domain_git::DiffLineType::Added),
        ("context", crowbar_proto::domain_git::DiffLineType::Context),
        ("header", crowbar_proto::domain_git::DiffLineType::Header),
        ("removed", crowbar_proto::domain_git::DiffLineType::Removed),
    ];
    for (wire, variant) in cases {
        let json = serde_json::to_string(wire).expect("quote the wire value");
        let parsed: crowbar_proto::domain_git::DiffLineType =
            serde_json::from_str(&json).expect("deserialise a declared constant");
        assert_eq!(parsed, variant);
        assert_eq!(serde_json::to_string(&variant).expect("serialise"), json);

        // A declared value must not fall through to the untagged variant.
        // This is the assertion that fails if `Other` is ever ordered
        // before the named variants: untagged matching is first-wins.
        assert_ne!(
            parsed,
            crowbar_proto::domain_git::DiffLineType::Other(wire.to_owned())
        );

        assert_eq!(variant.clone(), variant);
        assert!(!format!("{variant:?}").is_empty());
    }

    // Go's zero value is a legal value that no constant names, which is
    // the whole reason the untagged variant exists.
    let zero: crowbar_proto::domain_git::DiffLineType =
        serde_json::from_str("\"\"").expect("deserialise the zero value");
    assert_eq!(
        zero,
        crowbar_proto::domain_git::DiffLineType::Other(String::new())
    );
    assert_eq!(serde_json::to_string(&zero).expect("serialise"), "\"\"");

    // And a value added on the Go side after this was generated must widen
    // the set rather than fail the whole response.
    let unknown: crowbar_proto::domain_git::DiffLineType =
        serde_json::from_str("\"a-value-no-constant-declares\"")
            .expect("deserialise an undeclared value");
    assert_eq!(
        unknown,
        crowbar_proto::domain_git::DiffLineType::Other("a-value-no-constant-declares".to_owned())
    );
    assert_eq!(
        serde_json::to_string(&unknown).expect("serialise"),
        "\"a-value-no-constant-declares\"",
    );
    assert_eq!(unknown.clone(), unknown);
    assert!(!format!("{unknown:?}").is_empty());
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.FileDiff`, fully populated.
#[test]
fn domain_git_file_diff_wire_shape() {
    let value: crowbar_proto::domain_git::FileDiff = crowbar_proto::domain_git::FileDiff {
        file_path: String::from("s1"),
        old_path: Some(String::from("s2")),
        new_path: Some(String::from("s3")),
        is_new: false,
        is_deleted: true,
        is_renamed: false,
        is_binary: Some(true),
        is_image: Some(false),
        old_blob_base64: Some(String::from("s9")),
        new_blob_base64: Some(String::from("s10")),
        lines: vec![crowbar_proto::domain_git::DiffLine {
            line_type: crowbar_proto::domain_git::DiffLineType::Added,
            content: String::from("s11"),
            old_line_number: Some(12),
            new_line_number: Some(13),
            hunk_id: Some(String::from("s14")),
        }],
        additions: 15,
        deletions: 16,
        hunks: vec![crowbar_proto::domain_git::Hunk {
            hunk_id: String::from("s17"),
            header: String::from("s18"),
            start_line: 19,
            end_line: 20,
        }],
        uncommitted: true,
    };
    let wire = r#"{"file_path": "s1", "old_path": "s2", "new_path": "s3", "is_new": false, "is_deleted": true, "is_renamed": false, "is_binary": true, "is_image": false, "old_blob_base64": "s9", "new_blob_base64": "s10", "lines": [{"line_type": "added", "content": "s11", "old_line_number": 12, "new_line_number": 13, "hunkId": "s14"}], "additions": 15, "deletions": 16, "hunks": [{"hunkId": "s17", "header": "s18", "startLine": 19, "endLine": 20}], "uncommitted": true}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::FileDiff =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::FileDiff =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.FileDiff` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_file_diff_zero_values() {
    let value: crowbar_proto::domain_git::FileDiff = crowbar_proto::domain_git::FileDiff {
        file_path: String::new(),
        old_path: None,
        new_path: None,
        is_new: false,
        is_deleted: false,
        is_renamed: false,
        is_binary: None,
        is_image: None,
        old_blob_base64: None,
        new_blob_base64: None,
        lines: Vec::new(),
        additions: 0,
        deletions: 0,
        hunks: Vec::new(),
        uncommitted: false,
    };
    let wire = r#"{"file_path": "", "is_new": false, "is_deleted": false, "is_renamed": false, "lines": null, "additions": 0, "deletions": 0, "hunks": null, "uncommitted": false}"#;

    let parsed: crowbar_proto::domain_git::FileDiff =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"file_path": "", "is_new": false, "is_deleted": false, "is_renamed": false, "lines": [], "additions": 0, "deletions": 0, "hunks": [], "uncommitted": false}"#)
            .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::FileDiff =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.FileOutline`, fully populated.
#[test]
fn domain_git_file_outline_wire_shape() {
    let value: crowbar_proto::domain_git::FileOutline = crowbar_proto::domain_git::FileOutline {
        path: String::from("s1"),
        old_path: Some(String::from("s2")),
        hunks: vec![crowbar_proto::domain_git::HunkShape {
            old_start: 3,
            old_lines: 4,
            new_start: 5,
            new_lines: 6,
        }],
        is_partial: true,
        is_binary: false,
    };
    let wire = r#"{"path": "s1", "oldPath": "s2", "hunks": [{"oldStart": 3, "oldLines": 4, "newStart": 5, "newLines": 6}], "isPartial": true, "isBinary": false}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::FileOutline =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::FileOutline =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.FileOutline` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_file_outline_zero_values() {
    let value: crowbar_proto::domain_git::FileOutline = crowbar_proto::domain_git::FileOutline {
        path: String::new(),
        old_path: None,
        hunks: Vec::new(),
        is_partial: false,
        is_binary: false,
    };
    let wire = r#"{"path": "", "hunks": null, "isPartial": false, "isBinary": false}"#;

    let parsed: crowbar_proto::domain_git::FileOutline =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"path": "", "hunks": [], "isPartial": false, "isBinary": false}"#)
            .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::FileOutline =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// Every constant of `github.com/char2cs/crowbar/api/internal/domain/git.GitFileStatus`, by its exact wire value, plus the
/// open-set fallback a Go named string type requires.
#[test]
fn domain_git_git_file_status_variants() {
    let cases: [(&str, crowbar_proto::domain_git::GitFileStatus); 6] = [
        ("added", crowbar_proto::domain_git::GitFileStatus::Added),
        (
            "conflicted",
            crowbar_proto::domain_git::GitFileStatus::Conflicted,
        ),
        ("deleted", crowbar_proto::domain_git::GitFileStatus::Deleted),
        (
            "modified",
            crowbar_proto::domain_git::GitFileStatus::Modified,
        ),
        ("renamed", crowbar_proto::domain_git::GitFileStatus::Renamed),
        (
            "untracked",
            crowbar_proto::domain_git::GitFileStatus::Untracked,
        ),
    ];
    for (wire, variant) in cases {
        let json = serde_json::to_string(wire).expect("quote the wire value");
        let parsed: crowbar_proto::domain_git::GitFileStatus =
            serde_json::from_str(&json).expect("deserialise a declared constant");
        assert_eq!(parsed, variant);
        assert_eq!(serde_json::to_string(&variant).expect("serialise"), json);

        // A declared value must not fall through to the untagged variant.
        // This is the assertion that fails if `Other` is ever ordered
        // before the named variants: untagged matching is first-wins.
        assert_ne!(
            parsed,
            crowbar_proto::domain_git::GitFileStatus::Other(wire.to_owned())
        );

        assert_eq!(variant.clone(), variant);
        assert!(!format!("{variant:?}").is_empty());
    }

    // Go's zero value is a legal value that no constant names, which is
    // the whole reason the untagged variant exists.
    let zero: crowbar_proto::domain_git::GitFileStatus =
        serde_json::from_str("\"\"").expect("deserialise the zero value");
    assert_eq!(
        zero,
        crowbar_proto::domain_git::GitFileStatus::Other(String::new())
    );
    assert_eq!(serde_json::to_string(&zero).expect("serialise"), "\"\"");

    // And a value added on the Go side after this was generated must widen
    // the set rather than fail the whole response.
    let unknown: crowbar_proto::domain_git::GitFileStatus =
        serde_json::from_str("\"a-value-no-constant-declares\"")
            .expect("deserialise an undeclared value");
    assert_eq!(
        unknown,
        crowbar_proto::domain_git::GitFileStatus::Other("a-value-no-constant-declares".to_owned())
    );
    assert_eq!(
        serde_json::to_string(&unknown).expect("serialise"),
        "\"a-value-no-constant-declares\"",
    );
    assert_eq!(unknown.clone(), unknown);
    assert!(!format!("{unknown:?}").is_empty());
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.Hunk`, fully populated.
#[test]
fn domain_git_hunk_wire_shape() {
    let value: crowbar_proto::domain_git::Hunk = crowbar_proto::domain_git::Hunk {
        hunk_id: String::from("s1"),
        header: String::from("s2"),
        start_line: 3,
        end_line: 4,
    };
    let wire = r#"{"hunkId": "s1", "header": "s2", "startLine": 3, "endLine": 4}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::Hunk =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::Hunk = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.Hunk` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_hunk_zero_values() {
    let value: crowbar_proto::domain_git::Hunk = crowbar_proto::domain_git::Hunk {
        hunk_id: String::new(),
        header: String::new(),
        start_line: 0,
        end_line: 0,
    };
    let wire = r#"{"hunkId": "", "header": "", "startLine": 0, "endLine": 0}"#;

    let parsed: crowbar_proto::domain_git::Hunk =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::Hunk = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.HunkShape`, fully populated.
#[test]
fn domain_git_hunk_shape_wire_shape() {
    let value: crowbar_proto::domain_git::HunkShape = crowbar_proto::domain_git::HunkShape {
        old_start: 1,
        old_lines: 2,
        new_start: 3,
        new_lines: 4,
    };
    let wire = r#"{"oldStart": 1, "oldLines": 2, "newStart": 3, "newLines": 4}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::HunkShape =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::HunkShape =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.HunkShape` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_hunk_shape_zero_values() {
    let value: crowbar_proto::domain_git::HunkShape = crowbar_proto::domain_git::HunkShape {
        old_start: 0,
        old_lines: 0,
        new_start: 0,
        new_lines: 0,
    };
    let wire = r#"{"oldStart": 0, "oldLines": 0, "newStart": 0, "newLines": 0}"#;

    let parsed: crowbar_proto::domain_git::HunkShape =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::HunkShape =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.Identity`, fully populated.
#[test]
fn domain_git_identity_wire_shape() {
    let value: crowbar_proto::domain_git::Identity = crowbar_proto::domain_git::Identity {
        login: String::from("s1"),
        display_name: String::from("s2"),
        avatar_url: String::from("s3"),
    };
    let wire = r#"{"login": "s1", "displayName": "s2", "avatarUrl": "s3"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::Identity =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::Identity =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.Identity` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_identity_zero_values() {
    let value: crowbar_proto::domain_git::Identity = crowbar_proto::domain_git::Identity {
        login: String::new(),
        display_name: String::new(),
        avatar_url: String::new(),
    };
    let wire = r#"{"login": "", "displayName": "", "avatarUrl": ""}"#;

    let parsed: crowbar_proto::domain_git::Identity =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::Identity =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// Every constant of `github.com/char2cs/crowbar/api/internal/domain/git.MergeStrategy`, by its exact wire value, plus the
/// open-set fallback a Go named string type requires.
#[test]
fn domain_git_merge_strategy_variants() {
    let cases: [(&str, crowbar_proto::domain_git::MergeStrategy); 3] = [
        ("merge", crowbar_proto::domain_git::MergeStrategy::Merge),
        ("rebase", crowbar_proto::domain_git::MergeStrategy::Rebase),
        ("squash", crowbar_proto::domain_git::MergeStrategy::Squash),
    ];
    for (wire, variant) in cases {
        let json = serde_json::to_string(wire).expect("quote the wire value");
        let parsed: crowbar_proto::domain_git::MergeStrategy =
            serde_json::from_str(&json).expect("deserialise a declared constant");
        assert_eq!(parsed, variant);
        assert_eq!(serde_json::to_string(&variant).expect("serialise"), json);

        // A declared value must not fall through to the untagged variant.
        // This is the assertion that fails if `Other` is ever ordered
        // before the named variants: untagged matching is first-wins.
        assert_ne!(
            parsed,
            crowbar_proto::domain_git::MergeStrategy::Other(wire.to_owned())
        );

        assert_eq!(variant.clone(), variant);
        assert!(!format!("{variant:?}").is_empty());
    }

    // Go's zero value is a legal value that no constant names, which is
    // the whole reason the untagged variant exists.
    let zero: crowbar_proto::domain_git::MergeStrategy =
        serde_json::from_str("\"\"").expect("deserialise the zero value");
    assert_eq!(
        zero,
        crowbar_proto::domain_git::MergeStrategy::Other(String::new())
    );
    assert_eq!(serde_json::to_string(&zero).expect("serialise"), "\"\"");

    // And a value added on the Go side after this was generated must widen
    // the set rather than fail the whole response.
    let unknown: crowbar_proto::domain_git::MergeStrategy =
        serde_json::from_str("\"a-value-no-constant-declares\"")
            .expect("deserialise an undeclared value");
    assert_eq!(
        unknown,
        crowbar_proto::domain_git::MergeStrategy::Other("a-value-no-constant-declares".to_owned())
    );
    assert_eq!(
        serde_json::to_string(&unknown).expect("serialise"),
        "\"a-value-no-constant-declares\"",
    );
    assert_eq!(unknown.clone(), unknown);
    assert!(!format!("{unknown:?}").is_empty());
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.MultiFileDiff`, fully populated.
#[test]
fn domain_git_multi_file_diff_wire_shape() {
    let value: crowbar_proto::domain_git::MultiFileDiff =
        crowbar_proto::domain_git::MultiFileDiff {
            commit_hash: Some(String::from("s1")),
            commit_message: Some(String::from("s2")),
            commit_description: Some(String::from("s3")),
            commit_author: Some(String::from("s4")),
            commit_date: Some(String::from("2026-07-31T05:00:00.000000005Z")),
            files: vec![crowbar_proto::domain_git::FileDiff {
                file_path: String::from("s6"),
                old_path: Some(String::from("s7")),
                new_path: Some(String::from("s8")),
                is_new: true,
                is_deleted: false,
                is_renamed: true,
                is_binary: Some(false),
                is_image: Some(true),
                old_blob_base64: Some(String::from("s14")),
                new_blob_base64: Some(String::from("s15")),
                lines: vec![crowbar_proto::domain_git::DiffLine {
                    line_type: crowbar_proto::domain_git::DiffLineType::Added,
                    content: String::from("s16"),
                    old_line_number: Some(17),
                    new_line_number: Some(18),
                    hunk_id: Some(String::from("s19")),
                }],
                additions: 20,
                deletions: 21,
                hunks: vec![crowbar_proto::domain_git::Hunk {
                    hunk_id: String::from("s22"),
                    header: String::from("s23"),
                    start_line: 24,
                    end_line: 25,
                }],
                uncommitted: false,
            }],
            total_files: 27,
            total_additions: 28,
            total_deletions: 29,
        };
    let wire = r#"{"commitHash": "s1", "commitMessage": "s2", "commitDescription": "s3", "commitAuthor": "s4", "commitDate": "2026-07-31T05:00:00.000000005Z", "files": [{"file_path": "s6", "old_path": "s7", "new_path": "s8", "is_new": true, "is_deleted": false, "is_renamed": true, "is_binary": false, "is_image": true, "old_blob_base64": "s14", "new_blob_base64": "s15", "lines": [{"line_type": "added", "content": "s16", "old_line_number": 17, "new_line_number": 18, "hunkId": "s19"}], "additions": 20, "deletions": 21, "hunks": [{"hunkId": "s22", "header": "s23", "startLine": 24, "endLine": 25}], "uncommitted": false}], "totalFiles": 27, "totalAdditions": 28, "totalDeletions": 29}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::MultiFileDiff =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::MultiFileDiff =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.MultiFileDiff` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_multi_file_diff_zero_values() {
    let value: crowbar_proto::domain_git::MultiFileDiff =
        crowbar_proto::domain_git::MultiFileDiff {
            commit_hash: None,
            commit_message: None,
            commit_description: None,
            commit_author: None,
            commit_date: None,
            files: Vec::new(),
            total_files: 0,
            total_additions: 0,
            total_deletions: 0,
        };
    let wire = r#"{"files": null, "totalFiles": 0, "totalAdditions": 0, "totalDeletions": 0}"#;

    let parsed: crowbar_proto::domain_git::MultiFileDiff =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(
        r#"{"files": [], "totalFiles": 0, "totalAdditions": 0, "totalDeletions": 0}"#,
    )
    .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::MultiFileDiff =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.ReviewFileSummary`, fully populated.
#[test]
fn domain_git_review_file_summary_wire_shape() {
    let value: crowbar_proto::domain_git::ReviewFileSummary =
        crowbar_proto::domain_git::ReviewFileSummary {
            path: String::from("s1"),
            old_path: Some(String::from("s2")),
            status: crowbar_proto::domain_git::GitFileStatus::Added,
            additions: 3,
            deletions: 4,
            uncommitted: true,
            staged: false,
        };
    let wire = r#"{"path": "s1", "old_path": "s2", "status": "added", "additions": 3, "deletions": 4, "uncommitted": true, "staged": false}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::ReviewFileSummary =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::ReviewFileSummary =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.ReviewFileSummary` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_review_file_summary_zero_values() {
    let value: crowbar_proto::domain_git::ReviewFileSummary =
        crowbar_proto::domain_git::ReviewFileSummary {
            path: String::new(),
            old_path: None,
            status: crowbar_proto::domain_git::GitFileStatus::Other(String::new()),
            additions: 0,
            deletions: 0,
            uncommitted: false,
            staged: false,
        };
    let wire = r#"{"path": "", "status": "", "additions": 0, "deletions": 0, "uncommitted": false, "staged": false}"#;

    let parsed: crowbar_proto::domain_git::ReviewFileSummary =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::ReviewFileSummary =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.SearchHit`, fully populated.
#[test]
fn domain_git_search_hit_wire_shape() {
    let value: crowbar_proto::domain_git::SearchHit = crowbar_proto::domain_git::SearchHit {
        path: String::from("s1"),
        side: String::from("s2"),
        line_number: 3,
        preview: String::from("s4"),
    };
    let wire = r#"{"path": "s1", "side": "s2", "lineNumber": 3, "preview": "s4"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::SearchHit =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::SearchHit =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.SearchHit` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_search_hit_zero_values() {
    let value: crowbar_proto::domain_git::SearchHit = crowbar_proto::domain_git::SearchHit {
        path: String::new(),
        side: String::new(),
        line_number: 0,
        preview: String::new(),
    };
    let wire = r#"{"path": "", "side": "", "lineNumber": 0, "preview": ""}"#;

    let parsed: crowbar_proto::domain_git::SearchHit =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::SearchHit =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/git.Stash`, fully populated.
#[test]
fn domain_git_stash_wire_shape() {
    let value: crowbar_proto::domain_git::Stash = crowbar_proto::domain_git::Stash {
        id: String::from("s1"),
        message: String::from("s2"),
        date: String::from("2026-07-31T05:00:00.000000003Z"),
        files_changed: 4,
    };
    let wire = r#"{"id": "s1", "message": "s2", "date": "2026-07-31T05:00:00.000000003Z", "filesChanged": 4}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_git::Stash =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::Stash = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/git.Stash` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_git_stash_zero_values() {
    let value: crowbar_proto::domain_git::Stash = crowbar_proto::domain_git::Stash {
        id: String::new(),
        message: String::new(),
        date: String::new(),
        files_changed: 0,
    };
    let wire = r#"{"id": "", "message": "", "date": "", "filesChanged": 0}"#;

    let parsed: crowbar_proto::domain_git::Stash =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_git::Stash = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/lsp.Diagnostic`, fully populated.
#[test]
fn domain_lsp_diagnostic_wire_shape() {
    let value: crowbar_proto::domain_lsp::Diagnostic = crowbar_proto::domain_lsp::Diagnostic {
        file_path: String::from("s1"),
        range: crowbar_proto::domain_lsp::Range {
            start: crowbar_proto::domain_lsp::Position {
                line: 2,
                character: 3,
            },
            end: crowbar_proto::domain_lsp::Position {
                line: 4,
                character: 5,
            },
        },
        severity: String::from("s6"),
        message: String::from("s7"),
        source: Some(String::from("s8")),
        code: Some(String::from("s9")),
    };
    let wire = r#"{"filePath": "s1", "range": {"start": {"line": 2, "character": 3}, "end": {"line": 4, "character": 5}}, "severity": "s6", "message": "s7", "source": "s8", "code": "s9"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_lsp::Diagnostic =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::Diagnostic =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/lsp.Diagnostic` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_lsp_diagnostic_zero_values() {
    let value: crowbar_proto::domain_lsp::Diagnostic = crowbar_proto::domain_lsp::Diagnostic {
        file_path: String::new(),
        range: crowbar_proto::domain_lsp::Range {
            start: crowbar_proto::domain_lsp::Position {
                line: 0,
                character: 0,
            },
            end: crowbar_proto::domain_lsp::Position {
                line: 0,
                character: 0,
            },
        },
        severity: String::new(),
        message: String::new(),
        source: None,
        code: None,
    };
    let wire = r#"{"filePath": "", "range": {"start": {"line": 0, "character": 0}, "end": {"line": 0, "character": 0}}, "severity": "", "message": ""}"#;

    let parsed: crowbar_proto::domain_lsp::Diagnostic =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::Diagnostic =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/lsp.DiagnosticsEvent`, fully populated.
#[test]
fn domain_lsp_diagnostics_event_wire_shape() {
    let value: crowbar_proto::domain_lsp::DiagnosticsEvent =
        crowbar_proto::domain_lsp::DiagnosticsEvent {
            ws_id: String::from("s1"),
            diagnostics: vec![crowbar_proto::domain_lsp::Diagnostic {
                file_path: String::from("s2"),
                range: crowbar_proto::domain_lsp::Range {
                    start: crowbar_proto::domain_lsp::Position {
                        line: 3,
                        character: 4,
                    },
                    end: crowbar_proto::domain_lsp::Position {
                        line: 5,
                        character: 6,
                    },
                },
                severity: String::from("s7"),
                message: String::from("s8"),
                source: Some(String::from("s9")),
                code: Some(String::from("s10")),
            }],
        };
    let wire = r#"{"wsId": "s1", "diagnostics": [{"filePath": "s2", "range": {"start": {"line": 3, "character": 4}, "end": {"line": 5, "character": 6}}, "severity": "s7", "message": "s8", "source": "s9", "code": "s10"}]}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_lsp::DiagnosticsEvent =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::DiagnosticsEvent =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/lsp.DiagnosticsEvent` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_lsp_diagnostics_event_zero_values() {
    let value: crowbar_proto::domain_lsp::DiagnosticsEvent =
        crowbar_proto::domain_lsp::DiagnosticsEvent {
            ws_id: String::new(),
            diagnostics: Vec::new(),
        };
    let wire = r#"{"wsId": "", "diagnostics": null}"#;

    let parsed: crowbar_proto::domain_lsp::DiagnosticsEvent =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(r#"{"wsId": "", "diagnostics": []}"#)
        .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::DiagnosticsEvent =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/lsp.Location`, fully populated.
#[test]
fn domain_lsp_location_wire_shape() {
    let value: crowbar_proto::domain_lsp::Location = crowbar_proto::domain_lsp::Location {
        file_path: String::from("s1"),
        range: crowbar_proto::domain_lsp::Range {
            start: crowbar_proto::domain_lsp::Position {
                line: 2,
                character: 3,
            },
            end: crowbar_proto::domain_lsp::Position {
                line: 4,
                character: 5,
            },
        },
    };
    let wire = r#"{"filePath": "s1", "range": {"start": {"line": 2, "character": 3}, "end": {"line": 4, "character": 5}}}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_lsp::Location =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::Location =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/lsp.Location` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_lsp_location_zero_values() {
    let value: crowbar_proto::domain_lsp::Location = crowbar_proto::domain_lsp::Location {
        file_path: String::new(),
        range: crowbar_proto::domain_lsp::Range {
            start: crowbar_proto::domain_lsp::Position {
                line: 0,
                character: 0,
            },
            end: crowbar_proto::domain_lsp::Position {
                line: 0,
                character: 0,
            },
        },
    };
    let wire = r#"{"filePath": "", "range": {"start": {"line": 0, "character": 0}, "end": {"line": 0, "character": 0}}}"#;

    let parsed: crowbar_proto::domain_lsp::Location =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::Location =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/lsp.Position`, fully populated.
#[test]
fn domain_lsp_position_wire_shape() {
    let value: crowbar_proto::domain_lsp::Position = crowbar_proto::domain_lsp::Position {
        line: 1,
        character: 2,
    };
    let wire = r#"{"line": 1, "character": 2}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_lsp::Position =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::Position =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/lsp.Position` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_lsp_position_zero_values() {
    let value: crowbar_proto::domain_lsp::Position = crowbar_proto::domain_lsp::Position {
        line: 0,
        character: 0,
    };
    let wire = r#"{"line": 0, "character": 0}"#;

    let parsed: crowbar_proto::domain_lsp::Position =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::Position =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/lsp.Range`, fully populated.
#[test]
fn domain_lsp_range_wire_shape() {
    let value: crowbar_proto::domain_lsp::Range = crowbar_proto::domain_lsp::Range {
        start: crowbar_proto::domain_lsp::Position {
            line: 1,
            character: 2,
        },
        end: crowbar_proto::domain_lsp::Position {
            line: 3,
            character: 4,
        },
    };
    let wire = r#"{"start": {"line": 1, "character": 2}, "end": {"line": 3, "character": 4}}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_lsp::Range =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::Range = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/lsp.Range` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_lsp_range_zero_values() {
    let value: crowbar_proto::domain_lsp::Range = crowbar_proto::domain_lsp::Range {
        start: crowbar_proto::domain_lsp::Position {
            line: 0,
            character: 0,
        },
        end: crowbar_proto::domain_lsp::Position {
            line: 0,
            character: 0,
        },
    };
    let wire = r#"{"start": {"line": 0, "character": 0}, "end": {"line": 0, "character": 0}}"#;

    let parsed: crowbar_proto::domain_lsp::Range =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::Range = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/lsp.TextEdit`, fully populated.
#[test]
fn domain_lsp_text_edit_wire_shape() {
    let value: crowbar_proto::domain_lsp::TextEdit = crowbar_proto::domain_lsp::TextEdit {
        range: crowbar_proto::domain_lsp::Range {
            start: crowbar_proto::domain_lsp::Position {
                line: 1,
                character: 2,
            },
            end: crowbar_proto::domain_lsp::Position {
                line: 3,
                character: 4,
            },
        },
        new_text: String::from("s5"),
    };
    let wire = r#"{"range": {"start": {"line": 1, "character": 2}, "end": {"line": 3, "character": 4}}, "newText": "s5"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_lsp::TextEdit =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::TextEdit =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/lsp.TextEdit` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_lsp_text_edit_zero_values() {
    let value: crowbar_proto::domain_lsp::TextEdit = crowbar_proto::domain_lsp::TextEdit {
        range: crowbar_proto::domain_lsp::Range {
            start: crowbar_proto::domain_lsp::Position {
                line: 0,
                character: 0,
            },
            end: crowbar_proto::domain_lsp::Position {
                line: 0,
                character: 0,
            },
        },
        new_text: String::new(),
    };
    let wire = r#"{"range": {"start": {"line": 0, "character": 0}, "end": {"line": 0, "character": 0}}, "newText": ""}"#;

    let parsed: crowbar_proto::domain_lsp::TextEdit =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::TextEdit =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/domain/lsp.WorkspaceEdit`, fully populated.
#[test]
fn domain_lsp_workspace_edit_wire_shape() {
    let value: crowbar_proto::domain_lsp::WorkspaceEdit =
        crowbar_proto::domain_lsp::WorkspaceEdit {
            changes: {
                let mut m = std::collections::HashMap::new();
                m.insert(
                    String::from("s1"),
                    vec![crowbar_proto::domain_lsp::TextEdit {
                        range: crowbar_proto::domain_lsp::Range {
                            start: crowbar_proto::domain_lsp::Position {
                                line: 2,
                                character: 3,
                            },
                            end: crowbar_proto::domain_lsp::Position {
                                line: 4,
                                character: 5,
                            },
                        },
                        new_text: String::from("s6"),
                    }],
                );
                m
            },
        };
    let wire = r#"{"changes": {"s1": [{"range": {"start": {"line": 2, "character": 3}, "end": {"line": 4, "character": 5}}, "newText": "s6"}]}}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::domain_lsp::WorkspaceEdit =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::WorkspaceEdit =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/domain/lsp.WorkspaceEdit` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn domain_lsp_workspace_edit_zero_values() {
    let value: crowbar_proto::domain_lsp::WorkspaceEdit =
        crowbar_proto::domain_lsp::WorkspaceEdit {
            changes: std::collections::HashMap::new(),
        };
    let wire = r#"{"changes": null}"#;

    let parsed: crowbar_proto::domain_lsp::WorkspaceEdit =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"changes": {}}"#).expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::domain_lsp::WorkspaceEdit =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/engine/provider/types.PRInfo`, fully populated.
#[test]
fn engine_provider_types_pr_info_wire_shape() {
    let value: crowbar_proto::engine_provider_types::PRInfo =
        crowbar_proto::engine_provider_types::PRInfo {
            number: 1,
            status: String::from("s2"),
            url: String::from("s3"),
            title: String::from("s4"),
            target_branch: String::from("s5"),
        };
    let wire = r#"{"number": 1, "status": "s2", "url": "s3", "title": "s4", "targetBranch": "s5"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::engine_provider_types::PRInfo =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::engine_provider_types::PRInfo =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/engine/provider/types.PRInfo` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn engine_provider_types_pr_info_zero_values() {
    let value: crowbar_proto::engine_provider_types::PRInfo =
        crowbar_proto::engine_provider_types::PRInfo {
            number: 0,
            status: String::new(),
            url: String::new(),
            title: String::new(),
            target_branch: String::new(),
        };
    let wire = r#"{"number": 0, "status": "", "url": "", "title": "", "targetBranch": ""}"#;

    let parsed: crowbar_proto::engine_provider_types::PRInfo =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::engine_provider_types::PRInfo =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/engine/provider/types.ProviderState`, fully populated.
#[test]
fn engine_provider_types_provider_state_wire_shape() {
    let value: crowbar_proto::engine_provider_types::ProviderState =
        crowbar_proto::engine_provider_types::ProviderState {
            protected: true,
            pr: Some(crowbar_proto::engine_provider_types::PRInfo {
                number: 2,
                status: String::from("s3"),
                url: String::from("s4"),
                title: String::from("s5"),
                target_branch: String::from("s6"),
            }),
        };
    let wire = r#"{"protected": true, "pr": {"number": 2, "status": "s3", "url": "s4", "title": "s5", "targetBranch": "s6"}}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::engine_provider_types::ProviderState =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::engine_provider_types::ProviderState =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/engine/provider/types.ProviderState` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn engine_provider_types_provider_state_zero_values() {
    let value: crowbar_proto::engine_provider_types::ProviderState =
        crowbar_proto::engine_provider_types::ProviderState {
            protected: false,
            pr: None,
        };
    let wire = r#"{"protected": false}"#;

    let parsed: crowbar_proto::engine_provider_types::ProviderState =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::engine_provider_types::ProviderState =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/engine/search.SearchResponse`, fully populated.
#[test]
fn engine_search_search_response_wire_shape() {
    let value: crowbar_proto::engine_search::SearchResponse =
        crowbar_proto::engine_search::SearchResponse {
            results: vec![crowbar_proto::engine_search::SearchResult {
                file_path: String::from("s1"),
                line_number: 2,
                line_text: String::from("s3"),
                match_start: 4,
                match_end: 5,
            }],
            truncated: false,
        };
    let wire = r#"{"results": [{"filePath": "s1", "lineNumber": 2, "lineText": "s3", "matchStart": 4, "matchEnd": 5}], "truncated": false}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::engine_search::SearchResponse =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::engine_search::SearchResponse =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/engine/search.SearchResponse` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn engine_search_search_response_zero_values() {
    let value: crowbar_proto::engine_search::SearchResponse =
        crowbar_proto::engine_search::SearchResponse {
            results: Vec::new(),
            truncated: false,
        };
    let wire = r#"{"results": null, "truncated": false}"#;

    let parsed: crowbar_proto::engine_search::SearchResponse =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value =
        serde_json::from_str(r#"{"results": [], "truncated": false}"#)
            .expect("parse the expected shape");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::engine_search::SearchResponse =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/engine/search.SearchResult`, fully populated.
#[test]
fn engine_search_search_result_wire_shape() {
    let value: crowbar_proto::engine_search::SearchResult =
        crowbar_proto::engine_search::SearchResult {
            file_path: String::from("s1"),
            line_number: 2,
            line_text: String::from("s3"),
            match_start: 4,
            match_end: 5,
        };
    let wire =
        r#"{"filePath": "s1", "lineNumber": 2, "lineText": "s3", "matchStart": 4, "matchEnd": 5}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::engine_search::SearchResult =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::engine_search::SearchResult =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/engine/search.SearchResult` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn engine_search_search_result_zero_values() {
    let value: crowbar_proto::engine_search::SearchResult =
        crowbar_proto::engine_search::SearchResult {
            file_path: String::new(),
            line_number: 0,
            line_text: String::new(),
            match_start: 0,
            match_end: 0,
        };
    let wire =
        r#"{"filePath": "", "lineNumber": 0, "lineText": "", "matchStart": 0, "matchEnd": 0}"#;

    let parsed: crowbar_proto::engine_search::SearchResult =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::engine_search::SearchResult =
        serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}

/// The wire shape of `github.com/char2cs/crowbar/api/internal/perf.Sample`, fully populated.
#[test]
fn perf_sample_wire_shape() {
    let value: crowbar_proto::perf::Sample = crowbar_proto::perf::Sample {
        name: String::from("s1"),
        duration_ms: 2.5,
        at: String::from("2026-07-31T05:00:00.000000003Z"),
    };
    let wire = r#"{"name": "s1", "durationMs": 2.5, "at": "2026-07-31T05:00:00.000000003Z"}"#;

    // The daemon's key names map onto these fields, and onto no others.
    let parsed: crowbar_proto::perf::Sample =
        serde_json::from_str(wire).expect("deserialise the populated fixture");
    assert_eq!(parsed, value);

    // ... and serialising reproduces that same shape, key for key.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    // serialize -> deserialize -> compare.
    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::perf::Sample = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);

    // The rest of the derive set.
    assert_eq!(value.clone(), value);
    assert!(!format!("{value:?}").is_empty());
}

/// `github.com/char2cs/crowbar/api/internal/perf.Sample` at Go's zero values: every optional field absent, and
/// `null` wherever a nil Go slice or map would marshal to one.
#[test]
fn perf_sample_zero_values() {
    let value: crowbar_proto::perf::Sample = crowbar_proto::perf::Sample {
        name: String::new(),
        duration_ms: 0.0,
        at: String::new(),
    };
    let wire = r#"{"name": "", "durationMs": 0.0, "at": ""}"#;

    let parsed: crowbar_proto::perf::Sample =
        serde_json::from_str(wire).expect("deserialise the zero-value fixture");
    assert_eq!(parsed, value);

    // Serialising normalises: an absent optional stays absent, and a null
    // container comes back out as an empty one rather than as null.
    let produced = serde_json::to_value(&value).expect("serialise");
    let expected: serde_json::Value = serde_json::from_str(wire).expect("parse the fixture");
    assert_eq!(produced, expected);

    let text = serde_json::to_string(&value).expect("serialise");
    let back: crowbar_proto::perf::Sample = serde_json::from_str(&text).expect("round trip");
    assert_eq!(back, value);
}
