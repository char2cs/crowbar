#![forbid(unsafe_code)]

//! `crowbar-app` — the Crowbar native binary.
//!
//! Scaffold only (item 0.1). Window and menu wiring, panes and routing land
//! here once `gpui` is vendored (item 0.2) and the two apps are proved to run
//! against one daemon on a shared `CROWBAR_HOME` (item 0.4).

fn main() {
    println!("crowbar-app: scaffold, no UI yet (native/QUEUE.md item 0.1)");
}
