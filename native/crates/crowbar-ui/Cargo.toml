[package]
name = "crowbar-ui"
description = "the Crowbar design system: Theme, sealed token newtypes, primitives"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
crowbar-core.workspace = true
# §4.2: `core`, `gpui`, `gpui-component`. Vendored and pinned by item 0.2, wired
# in by 0.4. Re-exported from `lib.rs` so the leaf view crates that §4.2 puts
# downstream of this one get the framework "transitively through crowbar-ui"
# without each restating the dependency.
gpui.workspace = true
gpui-component.workspace = true

[lints]
workspace = true
